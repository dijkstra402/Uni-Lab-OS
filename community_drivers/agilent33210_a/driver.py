"""
Auto-generated by Build Agent
Original: acq400_hapi/agilent33210.py

[UNILAB DEBUG] 此驱动已注入调试日志
- 每个方法被调用时会打印日志
- 日志格式: [UNILAB] ClassName.method_name() called
- 查看日志: logging.getLogger("unilab.driver").setLevel(logging.DEBUG)
"""
from typing import Dict, Any, Optional
import logging
import warnings as _warnings
_warnings.filterwarnings("ignore", message=r"pkg_resources is deprecated as an API\..*")
import sys as _sys
import types as _types

# 兼容性：部分上游框架（例如 lantz）在 import-time 强制加载 Qt（PyQt4/PySide1），
# 在 headless/现代 Python 环境中通常不存在这些绑定，会导致导入直接失败。
# 这里预注入一个最小的 `lantz.utils.qt` shim，避免触发其 Qt loader。
if "lantz.utils.qt" not in _sys.modules:
    _qt_mod = _types.ModuleType("lantz.utils.qt")

    class _DummyQtCore:
        class QObject:
            def __init__(self, *_args, **_kwargs):
                # 兼容 QObject(parent=...) 等调用
                return None

        @staticmethod
        def Signal(*_args, **_kwargs):
            return object()

        @staticmethod
        def Slot(*_args, **_kwargs):
            def _decorator(fn):
                return fn

            return _decorator

    _qt_mod.QtCore = _DummyQtCore

    class _DummySuperQObject(_DummyQtCore.QObject):
        def __new__(cls, *_args, **_kwargs):
            return super().__new__(cls)

        def __init__(self, *_args, **_kwargs):
            # 允许 Driver(...) 传参，不触发 object.__init__ 的 TypeError
            return None

    _qt_mod.MetaQObject = type
    _qt_mod.SuperQObject = _DummySuperQObject
    _sys.modules["lantz.utils.qt"] = _qt_mod

# Unilab 驱动调试日志
_unilab_logger = logging.getLogger("unilab.driver.Agilent33210A")
_unilab_logger.setLevel(logging.DEBUG)
if not _unilab_logger.handlers:
    _handler = logging.StreamHandler()
    _handler.setFormatter(logging.Formatter("[%(asctime)s] %(message)s"))
    _unilab_logger.addHandler(_handler)

# Vendored repo packages: acq400_hapi
import sys as _sys
from pathlib import Path as _Path
_vendor_root = _Path(__file__).resolve().parent / "_vendor"
if _vendor_root.exists() and str(_vendor_root) not in _sys.path:
    _sys.path.insert(0, str(_vendor_root))

"""
agilent33210 : SCPI insterface to function generator
"""

import os
import time
import socket

class Agilent33210A:
    def __init__(self, ipaddr):
        print("[UNILAB] Agilent33210A.__init__() called", flush=True)
        self.ipaddr = ipaddr
        self.socket = socket.socket()
        self.socket.connect((ipaddr, 5025))
        self.trace = int(os.getenv("A33210A_TRACE", "0"))

    def send(self, str):
        _unilab_logger.debug("[UNILAB] Agilent33210A.send() called")
        if self.trace:
            print("A33210:{} > {}".format(self.ipaddr, str))
        self.socket.send("{}\n".format(str).encode())

    def trigger(self):
        _unilab_logger.debug("[UNILAB] Agilent33210A.trigger() called")
        self.send("TRIG")

    def beep(self, sleep=0.5):
        _unilab_logger.debug("[UNILAB] Agilent33210A.beep() called")
        self.send("SYST:BEEP")
        time.sleep(sleep)
        self.send("SYST:BEEP")

    def config(self, freq, volts=1, shape="SIN"):
        _unilab_logger.debug("[UNILAB] Agilent33210A.config() called")
        self.send("VOLT {}".format(volts))
        self.send("OUTP:SYNC ON")
        self.send("FREQ {}".format(freq))
        self.send("FUNC:SHAP {}".format(shape))
        self.send("BURS:STAT OFF")
        self.send("TRIG:SOUR IMM")

    def config_burst(self, ncyc=1):
        _unilab_logger.debug("[UNILAB] Agilent33210A.config_burst() called")
        self.send("BURS:STAT ON")
        self.send("BURS:NCYC {}".format(ncyc))
        self.send("TRIG:SOUR BUS")

    def config_free_running_burst(self, ncyc=1, rate=1):
        _unilab_logger.debug("[UNILAB] Agilent33210A.config_free_running_burst() called")
        self.send("SYST:BEEP")
        self.send("OUTP OFF")
        self.send("TRIG:SOUR IMM")
        self.send("BURS:INT:PER {}".format(rate))
        self.send("BURS:NCYC {}".format(ncyc))
        self.send("BURS:MODE TRIG")
        self.send("OUTP ON")

__all__ = ["Agilent33210A"]
