"""
Auto-generated by Build Agent
Original: opendrop/vendor/harvesters/_private/core/port.py

[UNILAB DEBUG] 此驱动已注入调试日志
- 每个方法被调用时会打印日志
- 日志格式: [UNILAB] ClassName.method_name() called
- 查看日志: logging.getLogger("unilab.driver").setLevel(logging.DEBUG)
"""
from typing import Dict, Any, Optional
import logging
import warnings as _warnings
_warnings.filterwarnings("ignore", message=r"pkg_resources is deprecated as an API\..*")
import sys as _sys
import types as _types

# 兼容性：部分上游框架（例如 lantz）在 import-time 强制加载 Qt（PyQt4/PySide1），
# 在 headless/现代 Python 环境中通常不存在这些绑定，会导致导入直接失败。
# 这里预注入一个最小的 `lantz.utils.qt` shim，避免触发其 Qt loader。
if "lantz.utils.qt" not in _sys.modules:
    _qt_mod = _types.ModuleType("lantz.utils.qt")

    class _DummyQtCore:
        class QObject:
            def __init__(self, *_args, **_kwargs):
                # 兼容 QObject(parent=...) 等调用
                return None

        @staticmethod
        def Signal(*_args, **_kwargs):
            return object()

        @staticmethod
        def Slot(*_args, **_kwargs):
            def _decorator(fn):
                return fn

            return _decorator

    _qt_mod.QtCore = _DummyQtCore

    class _DummySuperQObject(_DummyQtCore.QObject):
        def __new__(cls, *_args, **_kwargs):
            return super().__new__(cls)

        def __init__(self, *_args, **_kwargs):
            # 允许 Driver(...) 传参，不触发 object.__init__ 的 TypeError
            return None

    _qt_mod.MetaQObject = type
    _qt_mod.SuperQObject = _DummySuperQObject
    _sys.modules["lantz.utils.qt"] = _qt_mod

# Unilab 驱动调试日志
_unilab_logger = logging.getLogger("unilab.driver.ConcretePort")
_unilab_logger.setLevel(logging.DEBUG)
if not _unilab_logger.handlers:
    _handler = logging.StreamHandler()
    _handler.setFormatter(logging.Formatter("[%(asctime)s] %(message)s"))
    _unilab_logger.addHandler(_handler)

# Vendored repo packages: opendrop
import sys as _sys
from pathlib import Path as _Path
_vendor_root = _Path(__file__).resolve().parent / "_vendor"
if _vendor_root.exists() and str(_vendor_root) not in _sys.path:
    _sys.path.insert(0, str(_vendor_root))


# ----------------------------------------------------------------------------
#
# Copyright 2018 EMVA
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#     http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.
#
# ----------------------------------------------------------------------------


# Standard library imports

# Related third party imports
from genicam.genapi import AbstractPort
from genicam.genapi import EAccessMode
from genicam.gentl import Port

# Local application/library specific imports


class ConcretePort(AbstractPort):
    """
    An object-oriented version of PortImpl class.

    """
    def __init__(self, port_object=None):
        print("[UNILAB] ConcretePort.__init__() called", flush=True)
        #
        super().__init__()

        #
        if isinstance(port_object, Port):
            self._port = port_object
        else:
            raise TypeError('Supplied object is not a Port object.')

    @property
    def port(self):
        _unilab_logger.debug("[UNILAB] ConcretePort.port() called")
        return self._port

    def is_open(self):
        _unilab_logger.debug("[UNILAB] ConcretePort.is_open() called")
        return False if self.port is None else True

    def write(self, address, value):
        _unilab_logger.debug("[UNILAB] ConcretePort.write() called")
        self.port.write(address, value)

    def read(self, address, length):
        _unilab_logger.debug("[UNILAB] ConcretePort.read() called")
        buffer = self.port.read(address, length)
        return buffer[1]

    def open(self, port_object):
        _unilab_logger.debug("[UNILAB] ConcretePort.open() called")
        self._port = port_object

    def close(self):
        _unilab_logger.debug("[UNILAB] ConcretePort.close() called")
        self._port = None

    def get_access_mode(self):
        _unilab_logger.debug("[UNILAB] ConcretePort.get_access_mode() called")
        return EAccessMode.RW if self.is_open() else EAccessMode.NA

__all__ = ["ConcretePort"]
