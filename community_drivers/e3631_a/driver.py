"""
Auto-generated by Build Agent
Original: lab_equipment/PSU_E3631A.py

[UNILAB DEBUG] 此驱动已注入调试日志
- 每个方法被调用时会打印日志
- 日志格式: [UNILAB] ClassName.method_name() called
- 查看日志: logging.getLogger("unilab.driver").setLevel(logging.DEBUG)
"""
from typing import Dict, Any, Optional
import logging
import warnings as _warnings
_warnings.filterwarnings("ignore", message=r"pkg_resources is deprecated as an API\..*")
import sys as _sys
import types as _types

# 兼容性：部分上游框架（例如 lantz）在 import-time 强制加载 Qt（PyQt4/PySide1），
# 在 headless/现代 Python 环境中通常不存在这些绑定，会导致导入直接失败。
# 这里预注入一个最小的 `lantz.utils.qt` shim，避免触发其 Qt loader。
if "lantz.utils.qt" not in _sys.modules:
    _qt_mod = _types.ModuleType("lantz.utils.qt")

    class _DummyQtCore:
        class QObject:
            def __init__(self, *_args, **_kwargs):
                # 兼容 QObject(parent=...) 等调用
                return None

        @staticmethod
        def Signal(*_args, **_kwargs):
            return object()

        @staticmethod
        def Slot(*_args, **_kwargs):
            def _decorator(fn):
                return fn

            return _decorator

    _qt_mod.QtCore = _DummyQtCore

    class _DummySuperQObject(_DummyQtCore.QObject):
        def __new__(cls, *_args, **_kwargs):
            return super().__new__(cls)

        def __init__(self, *_args, **_kwargs):
            # 允许 Driver(...) 传参，不触发 object.__init__ 的 TypeError
            return None

    _qt_mod.MetaQObject = type
    _qt_mod.SuperQObject = _DummySuperQObject
    _sys.modules["lantz.utils.qt"] = _qt_mod

# Unilab 驱动调试日志
_unilab_logger = logging.getLogger("unilab.driver.E3631A")
_unilab_logger.setLevel(logging.DEBUG)
if not _unilab_logger.handlers:
    _handler = logging.StreamHandler()
    _handler.setFormatter(logging.Formatter("[%(asctime)s] %(message)s"))
    _unilab_logger.addHandler(_handler)

# Vendored repo packages: lab_equipment
import sys as _sys
from pathlib import Path as _Path
_vendor_root = _Path(__file__).resolve().parent / "_vendor"
if _vendor_root.exists() and str(_vendor_root) not in _sys.path:
    _sys.path.insert(0, str(_vendor_root))

#python pyvisa commands for controlling Keysight E3631A series power supplies
#Commands: https://www.keysight.com/ca/en/assets/9018-01308/user-manuals/9018-01308.pdf?success=true

import pyvisa
import time
from lab_equipment.PyVisaDeviceTemplate import PowerSupplyDevice

# Power Supply
class E3631A(PowerSupplyDevice):
    def __init__(self, *args, **kwargs):
        print("[UNILAB] E3631A.__init__() called", flush=True)
        super().__init__(*args, **kwargs)
    has_remote_sense = False
    can_measure_v_while_off = True #Have not checked this.
    
    connection_settings = {
        'query_delay':           0.75,
        'pyvisa_backend':       '@ivi',
        'time_wait_after_open': 0,
        'idn_available':         True
    }
    
    # Initialize the E3631A Power Supply
    def initialize(self):
        _unilab_logger.debug("[UNILAB] E3631A.initialize() called")
        self.inst.write("*RST")
        
        split_string = self.inst_idn.split(",")
        self.manufacturer = split_string[0]
        self.model = split_string[1]
        self.zero_number = split_string[2]
        self.version_number = split_string[3]
        
        #Choose channel 2 by default
        self.select_channel(2)
        
        self.lock_front_panel(True)
        self.set_current(0)
        self.set_voltage(0)
        
    def select_channel(self, channel):
        _unilab_logger.debug("[UNILAB] E3631A.select_channel() called")
        #channel is a number - 1,2,3
        if(channel <= 3) and (channel > 0):
            self.inst.write("INST:NSEL {}".format(channel))
        else:
            print("Invalid Channel")
    
    def set_current(self, current_setpoint_A):
        _unilab_logger.debug("[UNILAB] E3631A.set_current() called")
        self.inst.write("CURR {}".format(current_setpoint_A))
    
    def set_voltage(self, voltage_setpoint_V):
        _unilab_logger.debug("[UNILAB] E3631A.set_voltage() called")
        self.inst.write("VOLT {}".format(voltage_setpoint_V))

    def toggle_output(self, state):
        _unilab_logger.debug("[UNILAB] E3631A.toggle_output() called")
        if state:
            self.inst.write("OUTP ON")
        else:
            self.inst.write("OUTP OFF")
    
    def remote_sense(self, state):
        _unilab_logger.debug("[UNILAB] E3631A.remote_sense() called")
        pass
    
    def lock_front_panel(self, state):
        _unilab_logger.debug("[UNILAB] E3631A.lock_front_panel() called")
        if state:
            self.inst.write("SYST:REM")
        else:
            self.inst.write("SYST:LOC")
    
    def measure_voltage(self):
        _unilab_logger.debug("[UNILAB] E3631A.measure_voltage() called")
        return float(self.inst.query("MEAS:VOLT?"))

    def measure_current(self):
        _unilab_logger.debug("[UNILAB] E3631A.measure_current() called")
        return float(self.inst.query("MEAS:CURR?"))
        
    def measure_power(self):
        _unilab_logger.debug("[UNILAB] E3631A.measure_power() called")
        return self.measure_current()*self.measure_voltage()
        
    def __del__(self):
        _unilab_logger.debug("[UNILAB] E3631A.__del__() called")
        try:
            for ch in range(3):
                self.lock_front_panel(False)
                self.select_channel(ch+1)
                self.toggle_output(False)
            self.inst.close()
        except (AttributeError, pyvisa.errors.InvalidSession):
            pass
