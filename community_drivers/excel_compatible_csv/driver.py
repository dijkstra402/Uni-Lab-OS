"""
Auto-generated by Build Agent
Original: pyscada/export/csv_file.py

[UNILAB DEBUG] 此驱动已注入调试日志
- 每个方法被调用时会打印日志
- 日志格式: [UNILAB] ClassName.method_name() called
- 查看日志: logging.getLogger("unilab.driver").setLevel(logging.DEBUG)
"""
from __future__ import unicode_literals

from typing import Dict, Any, Optional
import logging
import warnings as _warnings
_warnings.filterwarnings("ignore", message=r"pkg_resources is deprecated as an API\..*")
import sys as _sys
import types as _types

# 兼容性：部分上游框架（例如 lantz）在 import-time 强制加载 Qt（PyQt4/PySide1），
# 在 headless/现代 Python 环境中通常不存在这些绑定，会导致导入直接失败。
# 这里预注入一个最小的 `lantz.utils.qt` shim，避免触发其 Qt loader。
if "lantz.utils.qt" not in _sys.modules:
    _qt_mod = _types.ModuleType("lantz.utils.qt")

    class _DummyQtCore:
        class QObject:
            def __init__(self, *_args, **_kwargs):
                # 兼容 QObject(parent=...) 等调用
                return None

        @staticmethod
        def Signal(*_args, **_kwargs):
            return object()

        @staticmethod
        def Slot(*_args, **_kwargs):
            def _decorator(fn):
                return fn

            return _decorator

    _qt_mod.QtCore = _DummyQtCore

    class _DummySuperQObject(_DummyQtCore.QObject):
        def __new__(cls, *_args, **_kwargs):
            return super().__new__(cls)

        def __init__(self, *_args, **_kwargs):
            # 允许 Driver(...) 传参，不触发 object.__init__ 的 TypeError
            return None

    _qt_mod.MetaQObject = type
    _qt_mod.SuperQObject = _DummySuperQObject
    _sys.modules["lantz.utils.qt"] = _qt_mod

# Unilab 驱动调试日志
_unilab_logger = logging.getLogger("unilab.driver.ExcelCompatibleCSV")
_unilab_logger.setLevel(logging.DEBUG)
if not _unilab_logger.handlers:
    _handler = logging.StreamHandler()
    _handler.setFormatter(logging.Formatter("[%(asctime)s] %(message)s"))
    _unilab_logger.addHandler(_handler)

# Vendored repo packages: pyscada
import sys as _sys
from pathlib import Path as _Path
_vendor_root = _Path(__file__).resolve().parent / "_vendor"
if _vendor_root.exists() and str(_vendor_root) not in _sys.path:
    _sys.path.insert(0, str(_vendor_root))

import csv
import os
import logging

logger = logging.getLogger(__name__)


def unix_time_stamp_to_excel_datenum(timestamp):
    """
    convert from unix time (seconds since 01/01/1970) to Excel datenum (days since 01/01/1900)
    """
    return (timestamp / 86400.0) + 25569.0


class ExcelCompatibleCSV:
    def __init__(self, filename, **kwargs):
        print("[UNILAB] ExcelCompatibleCSV.__init__() called", flush=True)
        self.filename = os.path.expanduser(filename)
        self.data = {}
        self.header = {}
        self.data_rows = 0
        self.header_is_writen = False
        self.dialect = "excel"  # default is excel

    def write_data(self, name, data, **kwargs):
        _unilab_logger.debug("[UNILAB] ExcelCompatibleCSV.write_data() called")
        if name in self.data:
            # append data
            self.data[name] += data
            self.data_rows = max(self.data_rows, len(self.data[name]))
        else:
            # new entry
            self.data[name] = data
            self.data_rows = max(self.data_rows, len(self.data[name]))  # update len
            self.header[name] = kwargs.copy()

    def write_file(self):
        _unilab_logger.debug("[UNILAB] ExcelCompatibleCSV.write_file() called")
        # check the data
        # data has to have the same len
        del_keys = []
        for key in self.data:
            if len(self.data[key]) != self.data_rows:
                del_keys.append(key)
        for i in del_keys:
            self.data.pop(i, None)
            self.header.pop(i, None)
        # construct the write arrays
        keys = self.data.keys()
        output = zip(*self.data.values())
        # truncate file on first write, otherwise append
        write_mode = "a" if self.header_is_writen else "w"
        with open(self.filename, write_mode) as f:
            writer = csv.writer(f, dialect=self.dialect)
            if not self.header_is_writen:
                writer.writerow(keys)
                self.header_is_writen = True
            writer.writerows(output)
        # reset internal data
        self.data = {}
        self.data_rows = 0
        self.header = {}

    def reopen(self):
        _unilab_logger.debug("[UNILAB] ExcelCompatibleCSV.reopen() called")
        self.write_file()

    def close_file(self):
        _unilab_logger.debug("[UNILAB] ExcelCompatibleCSV.close_file() called")
        self.write_file()

__all__ = ["ExcelCompatibleCSV"]
