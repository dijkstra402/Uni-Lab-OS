"""
Auto-generated by Build Agent
Original: py4syn/writing/FileWriter.py

[UNILAB DEBUG] 此驱动已注入调试日志
- 每个方法被调用时会打印日志
- 日志格式: [UNILAB] ClassName.method_name() called
- 查看日志: logging.getLogger("unilab.driver").setLevel(logging.DEBUG)
"""
from typing import Dict, Any, Optional
import logging
import warnings as _warnings
_warnings.filterwarnings("ignore", message=r"pkg_resources is deprecated as an API\..*")
import sys as _sys
import types as _types

# 兼容性：部分上游框架（例如 lantz）在 import-time 强制加载 Qt（PyQt4/PySide1），
# 在 headless/现代 Python 环境中通常不存在这些绑定，会导致导入直接失败。
# 这里预注入一个最小的 `lantz.utils.qt` shim，避免触发其 Qt loader。
if "lantz.utils.qt" not in _sys.modules:
    _qt_mod = _types.ModuleType("lantz.utils.qt")

    class _DummyQtCore:
        class QObject:
            def __init__(self, *_args, **_kwargs):
                # 兼容 QObject(parent=...) 等调用
                return None

        @staticmethod
        def Signal(*_args, **_kwargs):
            return object()

        @staticmethod
        def Slot(*_args, **_kwargs):
            def _decorator(fn):
                return fn

            return _decorator

    _qt_mod.QtCore = _DummyQtCore

    class _DummySuperQObject(_DummyQtCore.QObject):
        def __new__(cls, *_args, **_kwargs):
            return super().__new__(cls)

        def __init__(self, *_args, **_kwargs):
            # 允许 Driver(...) 传参，不触发 object.__init__ 的 TypeError
            return None

    _qt_mod.MetaQObject = type
    _qt_mod.SuperQObject = _DummySuperQObject
    _sys.modules["lantz.utils.qt"] = _qt_mod

# Unilab 驱动调试日志
_unilab_logger = logging.getLogger("unilab.driver.FileWriter")
_unilab_logger.setLevel(logging.DEBUG)
if not _unilab_logger.handlers:
    _handler = logging.StreamHandler()
    _handler.setFormatter(logging.Formatter("[%(asctime)s] %(message)s"))
    _unilab_logger.addHandler(_handler)

# Vendored repo packages: py4syn
import sys as _sys
from pathlib import Path as _Path
_vendor_root = _Path(__file__).resolve().parent / "_vendor"
if _vendor_root.exists() and str(_vendor_root) not in _sys.path:
    _sys.path.insert(0, str(_vendor_root))

from abc import ABCMeta, abstractmethod

"""Standard File Writer Class

Python Class for File Writer functions and informations.

:platform: Unix
:synopsis: Python Class for Standard Device informations.

.. moduleauthor:: Hugo Henrique Slepicka <hugo.slepicka@lnls.br>

"""
class FileWriter:
    """
    Class to be inherited by all data output classes in order to be transparently used in scan functions.

    By default some informations such as username, start and end timestamp, command and others are already filled by the scan routines using the getters and setters bellow.
    """
    def __init__(self, fileName):
        print("[UNILAB] FileWriter.__init__() called", flush=True)
        """
        **Constructor**

        Parameters
        ----------
        fileName : `string`
            The output filename
        """
        self.__fileName = fileName

        self.__username = ""
        self.__command = ""
        self.__comments = []
        self.__startDate = None
        self.__endDate = None

        self.__dataSize = None
        self.__devices = []
        self.__signals = []
        self.__devicesData = {}
        self.__signalsData = {}

    @abstractmethod
    def writeHeader(self):
        _unilab_logger.debug("[UNILAB] FileWriter.writeHeader() called")
        """
        Abstract method to start the header write process.

        """
        raise NotImplementedError

    @abstractmethod
    def writeData(self, partial=False, idx=-1):
        _unilab_logger.debug("[UNILAB] FileWriter.writeData() called")
        """
        Abstract method to start the data write process.

        Parameters
        ----------
        channel : `int`
            If `partial` is **True** it means that only data at position `idx` must be written. Otherwise this function must write all data. 
        idx : `int`
            The data index to be saved

        """
        raise NotImplementedError

    @abstractmethod
    def close(self):
        _unilab_logger.debug("[UNILAB] FileWriter.close() called")
        """
        Abstract method called at the end of the process.

        """
        raise NotImplementedError

    def getFileName(self):
        _unilab_logger.debug("[UNILAB] FileWriter.getFileName() called")
        """
        Returns the filename in use.

        Returns
        -------
        `string`
        """
        return self.__fileName

    def getUsername(self):
        _unilab_logger.debug("[UNILAB] FileWriter.getUsername() called")
        """
        Returns the username.

        Returns
        -------
        `string`
        """
        return self.__username

    def getCommand(self):
        _unilab_logger.debug("[UNILAB] FileWriter.getCommand() called")
        """
        Returns the command.

        Returns
        -------
        `string`
        """
        return self.__command

    def getComments(self):
        _unilab_logger.debug("[UNILAB] FileWriter.getComments() called")
        """
        Returns the comments list.

        Returns
        -------
        `list`
        """
        return self.__comments

    def getStartDate(self):
        _unilab_logger.debug("[UNILAB] FileWriter.getStartDate() called")
        """
        Returns the process start timestamp.

        Returns
        -------
        `string`
        """
        return self.__startDate

    def getEndDate(self):
        _unilab_logger.debug("[UNILAB] FileWriter.getEndDate() called")
        """
        Returns the process end timestamp.

        Returns
        -------
        `string`
        """
        return self.__endDate

    def getDataSize(self):
        _unilab_logger.debug("[UNILAB] FileWriter.getDataSize() called")
        """
        Returns the data size in number of lines.

        Returns
        -------
        `int`
        """
        if self.__dataSize is None:
            s = [len(d) for _, d in self.__signalsData.items()]
            d = [len(d) for _, d in self.__devicesData.items()]
            self.__dataSize = max(s+d)
        return self.__dataSize

    def getDevices(self):
        _unilab_logger.debug("[UNILAB] FileWriter.getDevices() called")
        """
        Returns the list of devices mnemonics.

        Returns
        -------
        `list`
        """
        return self.__devices

    def getSignals(self):
        _unilab_logger.debug("[UNILAB] FileWriter.getSignals() called")
        """
        Returns the list of signals mnemonics.

        Returns
        -------
        `list`
        """
        return self.__signals

    def getDevicesData(self):
        _unilab_logger.debug("[UNILAB] FileWriter.getDevicesData() called")
        """
        Returns the map containing all devices data in a map where the device mnemonic is the key and the values are an array.

        Returns
        -------
        `map`
        """
        return self.__devicesData

    def getSignalsData(self):
        _unilab_logger.debug("[UNILAB] FileWriter.getSignalsData() called")
        """
        Returns the map containing all signals data in a map where the device mnemonic is the key and the values are an array.

        Returns
        -------
        `map`
        """
        return self.__signalsData

    def setUsername(self, value):
        _unilab_logger.debug("[UNILAB] FileWriter.setUsername() called")
        """
        Set the username information

        Parameters
        ----------
        value : `string`
            Username information
        """
        self.__username = value

    def setCommand(self, value):
        _unilab_logger.debug("[UNILAB] FileWriter.setCommand() called")
        """
        Set the command information

        Parameters
        ----------
        value : `string`
            Command information
        """
        self.__command = value

    def setComments(self, value):
        _unilab_logger.debug("[UNILAB] FileWriter.setComments() called")
        """
        Set the comments list

        Parameters
        ----------
        value : `list`
            Comments list
        """
        self.__comments = value

    def insertComment(self, value):
        _unilab_logger.debug("[UNILAB] FileWriter.insertComment() called")
        """
        Insert a new comment in the list

        Parameters
        ----------
        value : `string`
            Comment information
        """
        self.__comments.append(value)

    def setStartDate(self, value):
        _unilab_logger.debug("[UNILAB] FileWriter.setStartDate() called")
        """
        Set the start date

        Parameters
        ----------
        value : `string`
            Date when the process started
        """
        self.__startDate = value

    def setEndDate(self, value):
        _unilab_logger.debug("[UNILAB] FileWriter.setEndDate() called")
        """
        Set the end date

        Parameters
        ----------
        value : `string`
            Date when the process finished
        """
        self.__endDate = value

    def setDataSize(self, value):
        _unilab_logger.debug("[UNILAB] FileWriter.setDataSize() called")
        """
        Set the data size in number of lines

        Parameters
        ----------
        value : `int`
            Data size
        """
        self.__dataSize = value

    def setDevices(self, value):
        _unilab_logger.debug("[UNILAB] FileWriter.setDevices() called")
        """
        Set the devices list

        Parameters
        ----------
        value : `list`
            Devices Mnemonic list
        """
        self.__devices = value

    def insertDevice(self, value):
        _unilab_logger.debug("[UNILAB] FileWriter.insertDevice() called")
        """
        Insert a new device in the list

        Parameters
        ----------
        value : `string`
            Device mnemonic
        """
        self.__devices.append(value)

    def setSignals(self, value):
        _unilab_logger.debug("[UNILAB] FileWriter.setSignals() called")
        """
        Set the signals list

        Parameters
        ----------
        value : `list`
            Signals Mnemonic list
        """
        self.__signals = value

    def insertSignal(self, value):
        _unilab_logger.debug("[UNILAB] FileWriter.insertSignal() called")
        """
        Insert a new signal in the list

        Parameters
        ----------
        value : `string`
            Signal mnemonic
        """
        self.__signals.append(value)

    def setDevicesData(self, value):
        _unilab_logger.debug("[UNILAB] FileWriter.setDevicesData() called")
        """
        Set the devices data map

        Parameters
        ----------
        value : `map`
            Devices data map
        """
        self.__devicesData.append(value)

    def insertDeviceData(self, device, value):
        _unilab_logger.debug("[UNILAB] FileWriter.insertDeviceData() called")
        """
        Insert a new data value for the informed device in the map.

        Parameters
        ----------
        device : `string`
            Device mnemonic
        value : `float, int, str, etc`
            Collected value

        """
        if(device not in self.__devicesData.keys()):
            self.__devicesData[device] = [value]
        else:
            self.__devicesData[device].append(value)

    def setSignalsData(self, value):
        _unilab_logger.debug("[UNILAB] FileWriter.setSignalsData() called")
        """
        Set the signals data map

        Parameters
        ----------
        value : `map`
            Signals data map
        """
        self.__signalsData = value

    def insertSignalData(self, signal, value):
        _unilab_logger.debug("[UNILAB] FileWriter.insertSignalData() called")
        """
        Insert a new data value for the informed signal in the map.

        Parameters
        ----------
        signal : `string`
            Signal mnemonic
        value : `float, int, str, etc`
            Collected value

        """
        if(signal not in self.__signalsData.keys()):
            self.__signalsData[signal] = [value]
        else:
            self.__signalsData[signal].append(value)

__all__ = ["FileWriter"]
