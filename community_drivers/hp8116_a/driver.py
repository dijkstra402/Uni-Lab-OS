"""
Auto-generated by Build Agent
Original: src/labtoolkit/WaveformGenerator/HP8116A.py

[UNILAB DEBUG] 此驱动已注入调试日志
- 每个方法被调用时会打印日志
- 日志格式: [UNILAB] ClassName.method_name() called
- 查看日志: logging.getLogger("unilab.driver").setLevel(logging.DEBUG)
"""
from typing import Dict, Any, Optional
import logging
import warnings as _warnings
_warnings.filterwarnings("ignore", message=r"pkg_resources is deprecated as an API\..*")
import sys as _sys
import types as _types

# 兼容性：部分上游框架（例如 lantz）在 import-time 强制加载 Qt（PyQt4/PySide1），
# 在 headless/现代 Python 环境中通常不存在这些绑定，会导致导入直接失败。
# 这里预注入一个最小的 `lantz.utils.qt` shim，避免触发其 Qt loader。
if "lantz.utils.qt" not in _sys.modules:
    _qt_mod = _types.ModuleType("lantz.utils.qt")

    class _DummyQtCore:
        class QObject:
            def __init__(self, *_args, **_kwargs):
                # 兼容 QObject(parent=...) 等调用
                return None

        @staticmethod
        def Signal(*_args, **_kwargs):
            return object()

        @staticmethod
        def Slot(*_args, **_kwargs):
            def _decorator(fn):
                return fn

            return _decorator

    _qt_mod.QtCore = _DummyQtCore

    class _DummySuperQObject(_DummyQtCore.QObject):
        def __new__(cls, *_args, **_kwargs):
            return super().__new__(cls)

        def __init__(self, *_args, **_kwargs):
            # 允许 Driver(...) 传参，不触发 object.__init__ 的 TypeError
            return None

    _qt_mod.MetaQObject = type
    _qt_mod.SuperQObject = _DummySuperQObject
    _sys.modules["lantz.utils.qt"] = _qt_mod

# Unilab 驱动调试日志
_unilab_logger = logging.getLogger("unilab.driver.HP8116A")
_unilab_logger.setLevel(logging.DEBUG)
if not _unilab_logger.handlers:
    _handler = logging.StreamHandler()
    _handler.setFormatter(logging.Formatter("[%(asctime)s] %(message)s"))
    _unilab_logger.addHandler(_handler)

# Vendored repo packages: labtoolkit, labtoolkit
import sys as _sys
from pathlib import Path as _Path
_vendor_root = _Path(__file__).resolve().parent / "_vendor"
if _vendor_root.exists() and str(_vendor_root) not in _sys.path:
    _sys.path.insert(0, str(_vendor_root))

from labtoolkit.Instrument import Instrument


class HP8116A(Instrument):
    """HP 8116A, 0 to 50MHz.

    .. figure::  images/WaveformGenerator/HP8116A.jpg
    """
    """."""

    # self.amps = [0.01, 7]
    # self.freqs = [0, 50e6]

    # ' NO MESSAGE' or ' SYNTAX' on inital or following comms

    def __init__(self, *args, **kwargs):
        print("[UNILAB] HP8116A.__init__() called", flush=True)
        super().__init__(*args, **kwargs)
    def __post__(self):
        _unilab_logger.debug("[UNILAB] HP8116A.__post__() called")
        self.inst.read_termination = '\r\n'
        self.inst.write_termination = '\r\n'

    def state(self):
        _unilab_logger.debug("[UNILAB] HP8116A.state() called")
        """."""
        print(f"Amplitude: {self.amplitude}")
        print(f"Frequency: {self.frequency}")
        print(f"Shape: {self.shape}")
        # print(f"Load: {self.load}")
        # print("Output: {}".format(self.output))

    @property
    def frequency(self):
        _unilab_logger.debug("[UNILAB] HP8116A.frequency() called")
        """."""
        return self.query("IFRQ")

    @frequency.setter
    def frequency(self, frequency):
        _unilab_logger.debug("[UNILAB] HP8116A.frequency() called")
        self.write(f"FRQ {frequency} Hz")
 
    @property
    def shape(self):
        _unilab_logger.debug("[UNILAB] HP8116A.shape() called")
        """."""
        # inst.query('W3') # 0, 1,2,3 DC, Sine, Tri, Square
        return NotImplementedError # self.query("SOURce:FUNCtion:SHAPe?")

    @shape.setter
    def shape(self, shape="SIN"):
        _unilab_logger.debug("[UNILAB] HP8116A.shape() called")
        # self.write(f"SOURce:FUNCtion:SHAPe {shape}")
        pass

    @property
    def amplitude(self):
        _unilab_logger.debug("[UNILAB] HP8116A.amplitude() called")
        return self.query("IAMP")

    @amplitude.setter
    # @AmplitudeLimiter
    def amplitude(self, amplitude):
        _unilab_logger.debug("[UNILAB] HP8116A.amplitude() called")
        self.write(f"AMP {amplitude} V")

    @property
    def offset(self):
        _unilab_logger.debug("[UNILAB] HP8116A.offset() called")
        return self.query("IOFS")

    # @AmplitudeLimiter
    @offset.setter
    def offset(self, amplitude):
        _unilab_logger.debug("[UNILAB] HP8116A.offset() called")
        self.write(f"OFS {amplitude} V")

    @property
    def setup(self):
        _unilab_logger.debug("[UNILAB] HP8116A.setup() called")
        # ' M1,CT0,T0,W0,H0,A0,L0,C0,D0,FRQ 55.0 HZ,DTY   50  %,WID 500  MS,AMP 1.00  V,OFS 7.50  V,'
        setup_str = self.query('CST').strip()
        # 'M1,CT0,T0,W0,H0,A0,L0,C0,D0,FRQ 55.0 HZ,DTY   50  %,WID 500  MS,AMP 1.00  V,OFS 7.50  V,'
        
        flags = [x for x in setup_str.split(',') if len(x) == 2 or len(x) == 3]
        # ['M1', 'CT0', 'T0', 'W0', 'H0', 'A0', 'L0', 'C0', 'D0']

        values = [x for x in setup_str.split(',') if len(x) > 3]
        # ['FRQ 55.0 HZ', 'DTY   50  %', 'WID 500  MS', 'AMP 1.00  V', 'OFS 7.50  V']
        return setup_str
        

__all__ = ["HP8116A"]
