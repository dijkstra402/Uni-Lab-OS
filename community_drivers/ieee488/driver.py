"""
Auto-generated by Build Agent
Original: src/labtoolkit/IEEE488.py

[UNILAB DEBUG] 此驱动已注入调试日志
- 每个方法被调用时会打印日志
- 日志格式: [UNILAB] ClassName.method_name() called
- 查看日志: logging.getLogger("unilab.driver").setLevel(logging.DEBUG)
"""
from typing import Dict, Any, Optional
import logging
import warnings as _warnings
_warnings.filterwarnings("ignore", message=r"pkg_resources is deprecated as an API\..*")
import sys as _sys
import types as _types

# 兼容性：部分上游框架（例如 lantz）在 import-time 强制加载 Qt（PyQt4/PySide1），
# 在 headless/现代 Python 环境中通常不存在这些绑定，会导致导入直接失败。
# 这里预注入一个最小的 `lantz.utils.qt` shim，避免触发其 Qt loader。
if "lantz.utils.qt" not in _sys.modules:
    _qt_mod = _types.ModuleType("lantz.utils.qt")

    class _DummyQtCore:
        class QObject:
            def __init__(self, *_args, **_kwargs):
                # 兼容 QObject(parent=...) 等调用
                return None

        @staticmethod
        def Signal(*_args, **_kwargs):
            return object()

        @staticmethod
        def Slot(*_args, **_kwargs):
            def _decorator(fn):
                return fn

            return _decorator

    _qt_mod.QtCore = _DummyQtCore

    class _DummySuperQObject(_DummyQtCore.QObject):
        def __new__(cls, *_args, **_kwargs):
            return super().__new__(cls)

        def __init__(self, *_args, **_kwargs):
            # 允许 Driver(...) 传参，不触发 object.__init__ 的 TypeError
            return None

    _qt_mod.MetaQObject = type
    _qt_mod.SuperQObject = _DummySuperQObject
    _sys.modules["lantz.utils.qt"] = _qt_mod

# Unilab 驱动调试日志
_unilab_logger = logging.getLogger("unilab.driver.IEEE488")
_unilab_logger.setLevel(logging.DEBUG)
if not _unilab_logger.handlers:
    _handler = logging.StreamHandler()
    _handler.setFormatter(logging.Formatter("[%(asctime)s] %(message)s"))
    _unilab_logger.addHandler(_handler)

# Vendored repo packages: labtoolkit, labtoolkit
import sys as _sys
from pathlib import Path as _Path
_vendor_root = _Path(__file__).resolve().parent / "_vendor"
if _vendor_root.exists() and str(_vendor_root) not in _sys.path:
    _sys.path.insert(0, str(_vendor_root))

from labtoolkit.Instrument import Instrument
from pyvisa.constants import VI_GPIB_REN_ADDRESS_GTL


class IEEE488(Instrument):
    """IEEE 488 functions."""
    
    '''
    def __repr__(self):
        # IDN = ', '.join(self.IDN.replace(', ', ',').split(',')[0:3])  # Make, Model, Serial
        # return f"{type(self).__name__} on {self.inst.resource_name} with {IDN}"
        try:
            self._IDN_
        except AttributeError:
            self._IDN_ = ', '.join(self.IDN.replace(', ', ',').split(',')[0:3])
            # Make, Model, Serial
        finally:
            return f'{type(self).__name__} on {self.inst.resource_name} with {self._IDN_}'
    '''
    
    def __init__(self, *args, **kwargs):
        print("[UNILAB] IEEE488.__init__() called", flush=True)
        super().__init__(*args, **kwargs)
    @property
    def CLS(self):
        _unilab_logger.debug("[UNILAB] IEEE488.CLS() called")
        r"""\*CLS - Clear status."""
        self.write("*CLS")

    def ESE(self, value):
        _unilab_logger.debug("[UNILAB] IEEE488.ESE() called")
        r"""\*ESE <enable_value> - Event status enable."""
        self.write(f"*ESE {value}")

    @property
    def ESR(self):
        _unilab_logger.debug("[UNILAB] IEEE488.ESR() called")
        r"""\*ESR? - Event status register query."""
        return self.query("*ESR?")

    @property
    def IDN(self):
        _unilab_logger.debug("[UNILAB] IEEE488.IDN() called")
        r"""\*IDN? - Instrument identification."""
        return self.query("*IDN?")

    @property
    def OPC(self):
        _unilab_logger.debug("[UNILAB] IEEE488.OPC() called")
        r"""\*OPC? - Wait for current operation to complete."""
        return self.query("*OPC?")

    @OPC.setter
    def OPC(self, value):
        _unilab_logger.debug("[UNILAB] IEEE488.OPC() called")
        r"""\*OPC - Set operation complete bit."""
        self.write(f"*OPC {value}")

    @property
    def OPT(self):
        _unilab_logger.debug("[UNILAB] IEEE488.OPT() called")
        r"""\*OPT? - Show installed options."""
        return self.query("*OPT?").strip('"') # .split(',')

    def RCL(self, value=0):
        _unilab_logger.debug("[UNILAB] IEEE488.RCL() called")
        r"""\*RCL {0|1|2|3|4} - Recall instrument state."""
        return self.write(f"*RCL {value}")

    def RST(self):
        _unilab_logger.debug("[UNILAB] IEEE488.RST() called")
        r"""\*RST - Reset instrument."""
        return self.write("*RST") 

    @property
    def WAI(self):
        _unilab_logger.debug("[UNILAB] IEEE488.WAI() called")
        r"""\*WAI - Wait for all pending operations to complete."""
        return self.query("*WAI") 

    @property
    def PSC(self, boolean):
        _unilab_logger.debug("[UNILAB] IEEE488.PSC() called")
        r"""\*PSC {0|1} - Power-on status clear."""
        return self.query("*PSC".format(boolean))

    @property
    def STB(self):
        _unilab_logger.debug("[UNILAB] IEEE488.STB() called")
        r"""\*STB? - Read status byte."""
        return self.query("*STB?")

    @property
    def TRG(self):
        _unilab_logger.debug("[UNILAB] IEEE488.TRG() called")
        r"""\*TRG - Trigger command."""
        return self.write("*TRG") 

    @property
    def TST(self):
        _unilab_logger.debug("[UNILAB] IEEE488.TST() called")
        r"""\*TST? - Self-test."""
        return self.query("*TST?")

    def SAV(self, index):
        _unilab_logger.debug("[UNILAB] IEEE488.SAV() called")
        r"""\*SAV {0|1|2|3|4} - Save instrument state."""
        return self.query(f"*SAV{index}")

    def SRE(self, value):
        _unilab_logger.debug("[UNILAB] IEEE488.SRE() called")
        r"""\*SRE <enable_value> - Service request enable (enable bits in enable register of Status Byte Register group."""
        return self.write(f"*SRE{value}")

    def GTL(self):
        _unilab_logger.debug("[UNILAB] IEEE488.GTL() called")
        """Go To Local."""
        return self.inst.control_ren(VI_GPIB_REN_ADDRESS_GTL)
    
    @property
    def local(self):
        _unilab_logger.debug("[UNILAB] IEEE488.local() called")
        """Go To Local."""
        return self.inst.control_ren(VI_GPIB_REN_ADDRESS_GTL)

__all__ = ["IEEE488"]
