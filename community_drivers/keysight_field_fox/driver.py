"""
Auto-generated by Build Agent
Original: src/labtoolkit/NetworkAnalyser/KeysightFieldFox.py

[UNILAB DEBUG] 此驱动已注入调试日志
- 每个方法被调用时会打印日志
- 日志格式: [UNILAB] ClassName.method_name() called
- 查看日志: logging.getLogger("unilab.driver").setLevel(logging.DEBUG)
"""
from typing import Dict, Any, Optional
import logging
import warnings as _warnings
_warnings.filterwarnings("ignore", message=r"pkg_resources is deprecated as an API\..*")
import sys as _sys
import types as _types

# 兼容性：部分上游框架（例如 lantz）在 import-time 强制加载 Qt（PyQt4/PySide1），
# 在 headless/现代 Python 环境中通常不存在这些绑定，会导致导入直接失败。
# 这里预注入一个最小的 `lantz.utils.qt` shim，避免触发其 Qt loader。
if "lantz.utils.qt" not in _sys.modules:
    _qt_mod = _types.ModuleType("lantz.utils.qt")

    class _DummyQtCore:
        class QObject:
            def __init__(self, *_args, **_kwargs):
                # 兼容 QObject(parent=...) 等调用
                return None

        @staticmethod
        def Signal(*_args, **_kwargs):
            return object()

        @staticmethod
        def Slot(*_args, **_kwargs):
            def _decorator(fn):
                return fn

            return _decorator

    _qt_mod.QtCore = _DummyQtCore

    class _DummySuperQObject(_DummyQtCore.QObject):
        def __new__(cls, *_args, **_kwargs):
            return super().__new__(cls)

        def __init__(self, *_args, **_kwargs):
            # 允许 Driver(...) 传参，不触发 object.__init__ 的 TypeError
            return None

    _qt_mod.MetaQObject = type
    _qt_mod.SuperQObject = _DummySuperQObject
    _sys.modules["lantz.utils.qt"] = _qt_mod

# Unilab 驱动调试日志
_unilab_logger = logging.getLogger("unilab.driver.KeysightFieldFox")
_unilab_logger.setLevel(logging.DEBUG)
if not _unilab_logger.handlers:
    _handler = logging.StreamHandler()
    _handler.setFormatter(logging.Formatter("[%(asctime)s] %(message)s"))
    _unilab_logger.addHandler(_handler)

# Vendored repo packages: labtoolkit, labtoolkit
import sys as _sys
from pathlib import Path as _Path
_vendor_root = _Path(__file__).resolve().parent / "_vendor"
if _vendor_root.exists() and str(_vendor_root) not in _sys.path:
    _sys.path.insert(0, str(_vendor_root))

from labtoolkit.IEEE488 import IEEE488
from labtoolkit.SCPI import SCPI
import numpy as np


class KeysightFieldFox(IEEE488, SCPI):
    """Keysight FieldFox.

    .. figure::  images/NetworkAnalyser/KeysightFieldFoxN9928A.jpg
    """

    def __init__(self, instrument, logger=None):
        print("[UNILAB] KeysightFieldFox.__init__() called", flush=True)
        """."""
        super().__init__(instrument)
        # self.log = logging.getLogger(__name__)
        self.freqs = [30e3, 26.5e9]
        # self.log.info('Creating an instance of\t' + str(__class__))

        self.write("*CLS")  # clear error status

    @property
    def points(self):
        _unilab_logger.debug("[UNILAB] KeysightFieldFox.points() called")
        """Sweep Points, max 10,001."""
        return(self.query("SENS:SWE:POIN?"))

    @points.setter
    def points(self, points):
        _unilab_logger.debug("[UNILAB] KeysightFieldFox.points() called")
        self.write(f"SENS:SWE:POIN {points:.0f}")

    @property
    def start(self):
        _unilab_logger.debug("[UNILAB] KeysightFieldFox.start() called")
        """Start frequency."""
        return(self.query("SENS:FREQ:STAR?"))

    @start.setter
    def start(self, start):
        _unilab_logger.debug("[UNILAB] KeysightFieldFox.start() called")
        self.write(f"SENS:FREQ:STAR {start:.0f}Hz")

    @property
    def stop(self):
        _unilab_logger.debug("[UNILAB] KeysightFieldFox.stop() called")
        """Stop frequency."""
        return(self.query("SENS:FREQ:STOP?"))

    @stop.setter
    def stop(self, stop):
        _unilab_logger.debug("[UNILAB] KeysightFieldFox.stop() called")
        self.write(f"SENS:FREQ:STOP {stop:.0f}Hz")

    @property
    def bandwidth(self):
        _unilab_logger.debug("[UNILAB] KeysightFieldFox.bandwidth() called")
        """IF bandwidth."""
        return(self.query(":BWID?"))

    @bandwidth.setter
    def bandwidth(self, bandwidth):
        _unilab_logger.debug("[UNILAB] KeysightFieldFox.bandwidth() called")
        self.write(f":BWID {bandwidth:.0f}")

    @property
    def display(self):
        _unilab_logger.debug("[UNILAB] KeysightFieldFox.display() called")
        """."""
        return(self.query("DISP:ENAB?"))

    @display.setter
    def display(self, display):
        _unilab_logger.debug("[UNILAB] KeysightFieldFox.display() called")
        self.write(f"DISP:ENAB {display:d}")

    @property
    def trigger(self):
        _unilab_logger.debug("[UNILAB] KeysightFieldFox.trigger() called")
        """."""
        return(self.query("INIT:CONT?"))

    @trigger.setter
    def trigger(self, trigger):
        _unilab_logger.debug("[UNILAB] KeysightFieldFox.trigger() called")
        self.write(f"INIT:CONT {trigger:d}")

    @property
    def format(self):
        _unilab_logger.debug("[UNILAB] KeysightFieldFox.format() called")
        """."""
        return(self.query("CALC:SEL:FORM?"))

    @format.setter
    def format(self, form):
        _unilab_logger.debug("[UNILAB] KeysightFieldFox.format() called")
        self.write(f"CALC:SEL:FORM {form}")

    @property
    def sparameter(self):
        _unilab_logger.debug("[UNILAB] KeysightFieldFox.sparameter() called")
        """."""
        return(self.query("CALC:PAR1:DEF?"))

    @sparameter.setter
    def sparameter(self, sparameter="S11"):  # S21, S12, S22
        self.write(f"CALC:PAR1:DEF {sparameter}")

    def sweep(self):
        _unilab_logger.debug("[UNILAB] KeysightFieldFox.sparameter() called")
        """."""
        self.write("INIT")
        self.write("*WAI")

    def readRI(self):
        _unilab_logger.debug("[UNILAB] KeysightFieldFox.readRI() called")
        """."""
        self.write("CALC:DATA:SDAT?")
        answer = self.read(b'\n').decode('ascii')
        parsed = answer.strip().split(",")
        real = [float(parsed[i]) for i in range(0, len(parsed), 2)]
        imag = [float(parsed[i]) for i in range(1, len(parsed), 2)]
        return (real, imag)

    def readFormatted(self):
        _unilab_logger.debug("[UNILAB] KeysightFieldFox.readFormatted() called")
        """."""
        self.write("CALC:DATA:FDAT?")
        answer = self.read(b'\n').decode('ascii')
        parsed = answer.strip().split(",")
        return ([float(x) for x in parsed], [0.0] * len(parsed))
    # topfreq = freq + span/2
    # botfreq = freq - span/2

__all__ = ["KeysightFieldFox"]
