"""
Auto-generated by Build Agent
Original: src/labtoolkit/FieldStrength/LumiloopLSProbe.py

[UNILAB DEBUG] 此驱动已注入调试日志
- 每个方法被调用时会打印日志
- 日志格式: [UNILAB] ClassName.method_name() called
- 查看日志: logging.getLogger("unilab.driver").setLevel(logging.DEBUG)
"""
from typing import Dict, Any, Optional
import logging
import warnings as _warnings
_warnings.filterwarnings("ignore", message=r"pkg_resources is deprecated as an API\..*")
import sys as _sys
import types as _types

# 兼容性：部分上游框架（例如 lantz）在 import-time 强制加载 Qt（PyQt4/PySide1），
# 在 headless/现代 Python 环境中通常不存在这些绑定，会导致导入直接失败。
# 这里预注入一个最小的 `lantz.utils.qt` shim，避免触发其 Qt loader。
if "lantz.utils.qt" not in _sys.modules:
    _qt_mod = _types.ModuleType("lantz.utils.qt")

    class _DummyQtCore:
        class QObject:
            def __init__(self, *_args, **_kwargs):
                # 兼容 QObject(parent=...) 等调用
                return None

        @staticmethod
        def Signal(*_args, **_kwargs):
            return object()

        @staticmethod
        def Slot(*_args, **_kwargs):
            def _decorator(fn):
                return fn

            return _decorator

    _qt_mod.QtCore = _DummyQtCore

    class _DummySuperQObject(_DummyQtCore.QObject):
        def __new__(cls, *_args, **_kwargs):
            return super().__new__(cls)

        def __init__(self, *_args, **_kwargs):
            # 允许 Driver(...) 传参，不触发 object.__init__ 的 TypeError
            return None

    _qt_mod.MetaQObject = type
    _qt_mod.SuperQObject = _DummySuperQObject
    _sys.modules["lantz.utils.qt"] = _qt_mod

# Unilab 驱动调试日志
_unilab_logger = logging.getLogger("unilab.driver.LumiloopLSProbe")
_unilab_logger.setLevel(logging.DEBUG)
if not _unilab_logger.handlers:
    _handler = logging.StreamHandler()
    _handler.setFormatter(logging.Formatter("[%(asctime)s] %(message)s"))
    _unilab_logger.addHandler(_handler)

# Vendored repo packages: labtoolkit, labtoolkit
import sys as _sys
from pathlib import Path as _Path
_vendor_root = _Path(__file__).resolve().parent / "_vendor"
if _vendor_root.exists() and str(_vendor_root) not in _sys.path:
    _sys.path.insert(0, str(_vendor_root))

from labtoolkit.IEEE488 import IEEE488
from labtoolkit.SCPI import SCPI

class LumiloopLSProbe(IEEE488, SCPI):
    """."""

    def __init__(self, *args, **kwargs):
        print("[UNILAB] LumiloopLSProbe.__init__() called", flush=True)
        super().__init__(*args, **kwargs)
    def __post__(self):
        _unilab_logger.debug("[UNILAB] LumiloopLSProbe.__post__() called")
        pass
        # self.write(f':SYST:ERR:VERB {True:b}')  # Turn on verbose command errors

    @property
    def magnitude(self):
        _unilab_logger.debug("[UNILAB] LumiloopLSProbe.magnitude() called")
        return self.query_float(':MEASure:FProbe:Efield:MAG?')
    
    @property
    def magnitude_all(self):
        _unilab_logger.debug("[UNILAB] LumiloopLSProbe.magnitude_all() called")
        return self.query_ascii_values(':MEASure:FProbe:Efield:ALL?')
        '''
        # '0.0621137,0.0999184,0.107581,0.159422'
        # [E] = sqrt((x**2)+(y**2)+(z**2))
        x, y, z = 0.0621137, 0.0999184, 0.107581
        Emag = np.sqrt((x**2)+(y**2)+(z**2))
        # 0.1594223006585026
        '''
   
    @property
    def frequency(self):
        _unilab_logger.debug("[UNILAB] LumiloopLSProbe.frequency() called")
        return self.query_float(':SYSTem:FREQuency?')

    @frequency.setter
    def frequency(self, frequency):
        _unilab_logger.debug("[UNILAB] LumiloopLSProbe.frequency() called")
        self.write(f':SYSTem:FREQuency {frequency}')
            
    @property
    def mode(self):
        _unilab_logger.debug("[UNILAB] LumiloopLSProbe.mode() called")
        return self.query_int(':SYSTem:MODE?')

    @mode.setter
    def mode(self, mode):
        _unilab_logger.debug("[UNILAB] LumiloopLSProbe.mode() called")
        self.write(f':SYSTem:MODE {mode:0.0f}')

__all__ = ["LumiloopLSProbe"]
