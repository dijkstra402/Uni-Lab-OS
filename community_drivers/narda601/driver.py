"""
Auto-generated by Build Agent
Original: src/labtoolkit/FieldStrength/Narda601.py

[UNILAB DEBUG] 此驱动已注入调试日志
- 每个方法被调用时会打印日志
- 日志格式: [UNILAB] ClassName.method_name() called
- 查看日志: logging.getLogger("unilab.driver").setLevel(logging.DEBUG)
"""
from typing import Dict, Any, Optional
import logging
import warnings as _warnings
_warnings.filterwarnings("ignore", message=r"pkg_resources is deprecated as an API\..*")
import sys as _sys
import types as _types

# 兼容性：部分上游框架（例如 lantz）在 import-time 强制加载 Qt（PyQt4/PySide1），
# 在 headless/现代 Python 环境中通常不存在这些绑定，会导致导入直接失败。
# 这里预注入一个最小的 `lantz.utils.qt` shim，避免触发其 Qt loader。
if "lantz.utils.qt" not in _sys.modules:
    _qt_mod = _types.ModuleType("lantz.utils.qt")

    class _DummyQtCore:
        class QObject:
            def __init__(self, *_args, **_kwargs):
                # 兼容 QObject(parent=...) 等调用
                return None

        @staticmethod
        def Signal(*_args, **_kwargs):
            return object()

        @staticmethod
        def Slot(*_args, **_kwargs):
            def _decorator(fn):
                return fn

            return _decorator

    _qt_mod.QtCore = _DummyQtCore

    class _DummySuperQObject(_DummyQtCore.QObject):
        def __new__(cls, *_args, **_kwargs):
            return super().__new__(cls)

        def __init__(self, *_args, **_kwargs):
            # 允许 Driver(...) 传参，不触发 object.__init__ 的 TypeError
            return None

    _qt_mod.MetaQObject = type
    _qt_mod.SuperQObject = _DummySuperQObject
    _sys.modules["lantz.utils.qt"] = _qt_mod

# Unilab 驱动调试日志
_unilab_logger = logging.getLogger("unilab.driver.Narda601")
_unilab_logger.setLevel(logging.DEBUG)
if not _unilab_logger.handlers:
    _handler = logging.StreamHandler()
    _handler.setFormatter(logging.Formatter("[%(asctime)s] %(message)s"))
    _unilab_logger.addHandler(_handler)

# Vendored repo packages: labtoolkit, labtoolkit
import sys as _sys
from pathlib import Path as _Path
_vendor_root = _Path(__file__).resolve().parent / "_vendor"
if _vendor_root.exists() and str(_vendor_root) not in _sys.path:
    _sys.path.insert(0, str(_vendor_root))

import binascii
import math
import struct

from pprint import pprint

# import serial
# from serial.tools import list_ports

from labtoolkit.Instrument import Instrument


class Narda601(Instrument):
    """Narda601.

    .. figure::  images/FieldStrength/Narda601.jpg
    """

    def __init__(self, *args, **kwargs):
        print("[UNILAB] Narda601.__init__() called", flush=True)
        super().__init__(*args, **kwargs)
    def setup(self):
        _unilab_logger.debug("[UNILAB] Narda601.setup() called")
        """Send setup commands to probe."""
        pass

    def readback(self):
        _unilab_logger.debug("[UNILAB] Narda601.readback() called")
        """Readback Field Strength."""
        pass


    def binarytoVm(self, binary=b'T\x00\x00\x00\x00'):
        _unilab_logger.debug("[UNILAB] Narda601.binarytoVm() called")
        if binary[0] == 84:  # ASCII capital T, 54 as hex, 84 as decimal
            binary = binary[1:]  # Remove leading character from binary 'T'
            value = struct.unpack('<f', binary)[0]  # unpack from binary
            # print('Value: {}\t::\tBinary: {}'.format(math.sqrt(value), binary))  # print(Hex, Value, Binary)
            return math.sqrt(value)  # Square root of value
        else:
            return ValueError  # if data was not ‘T’ Total Field, return ValueError


    def returnstr(self, binary=b'T\x00\x00\x00\x00'):
        _unilab_logger.debug("[UNILAB] Narda601.returnstr() called")
        return binary[1:].decode()  # Remove leading character from binary and decode


    def decode_example(self, hexes):
        _unilab_logger.debug("[UNILAB] Narda601.decode_example() called")
        for hexstring in hexes:
            hexstring = hexstring.replace(' ', '')  # Remove spaces in hexstring
            bins = binascii.unhexlify(hexstring)  # encode to binary, as you would receive over serial
            print('{}\t::\t{}\t::\t{}'.format(hexstring, self.binarytoVm(bins), bins))  # print(Hex, Value, Binary)


    def query(self, ser, command):
        _unilab_logger.debug("[UNILAB] Narda601.query() called")
        ser.write(command.encode('ascii') + b'\r\n')
        return ser.readline()

'''

hexes = [
    '54 00 00 00 00',
    '54 30 45 B5 3E',
    '54 DD C3 B9 3F',
    '54 62 81 10 40',
    '54 7F 0A 52 40',
    '54 76 82 FB 40',
    '54 63 FE 0A 41',
    '54 2E EC D1 40',
    '54 30 43 B5 3E',
    '54 00 C7 B9 3F',
    '54 02 47 D8 3E',
    '54 02 91 B5 3F',
    '54 08 10 3C 3F',
    '54 11 22 2A 40',
    '54 16 4B 61 40',
    '54 19 EA 20 41',
    '54 26 5C 34 40',
    '54 27 9B D8 3E',
    '54 28 7E ED 40',
    '54 2E EC D1 40',
    '54 30 43 B5 3E',
    '54 30 43 B5 3E',
    '54 4E 97 D0 3F',
    '54 52 85 11 3F',
    '54 52 85 11 3F',
    '54 53 EE EF 3F',
    '54 62 81 10 40',
    '54 63 FE 0A 41',
    '54 6A 01 E7 3F',
    '54 6C 8B 74 3F',
    '54 70 4F DD 3F',
    '54 76 82 FB 40',
    '54 77 13 29 40',
    '54 77 13 29 40',
    '54 78 CD 40 40',
    '54 78 CD 40 40',
    '54 7B 1F 52 3F',
    '54 7B 1F 52 3F',
    '54 7F 0A 52 40',
    '54 83 58 AE 3E',
    '54 83 58 AE 3E',
    '54 8C DC E5 40',
    '54 9E 4F 01 40',
    '54 9F 14 C7 3F',
    '54 A4 82 95 3F',
    '54 A7 D6 04 40',
    '54 A9 33 72 40',
    '54 AF B3 02 3F',
    '54 B4 BA 27 40',
    '54 BE E3 39 40',
    '54 C2 4F 43 3F',
    '54 C8 89 02 40',
    '54 D0 47 08 3D',
    '54 D3 C5 08 40',
    '54 DE A5 94 3F',
    '54 F5 B7 04 3F',
]



menu = {}
menu[1] = ['Read total field', '#00?T*', binarytoVm]
menu[2] = ['Read serial number', '#00?s*', returnstr]
menu[3] = ['Read version number', '#00?v*', returnstr]
menu[4] = ['Read part? number', '#00?p*', returnstr]

# menu[5] = ['Read battery voltage - no decoder', '#00?b*', returnstr]
menu[9] = ['Decode example hex values', None, decode_example]



# pprint(menu)
for each in list_ports.comports():
    print('{}\t\t{}'.format(each.device, each.description))
port = input('Serial Port? : ')

with serial.Serial(port, 9600, timeout=1, bytesize=8, parity='N', stopbits=1) as ser:
    while ser.isOpen():
        print()
        for entry in sorted(menu):
            print('{}: {}'.format(str(entry), menu[entry][0]))
        try:
            selection = int(input('\nSelection: '))
        except ValueError as e:
            selection = -1
        finally:
            try:
                if menu[selection][1] is not None:
                    print(menu[selection][2](query(ser, menu[selection][1])))
                else:
                    menu[selection][2]()
            except KeyError as e:
                print('Unknown selection: {}'.format(e))
            
'''

__all__ = ["Narda601"]
