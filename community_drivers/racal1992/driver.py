"""
Auto-generated by Build Agent
Original: src/labtoolkit/FrequencyCounter/Racal1992.py

[UNILAB DEBUG] 此驱动已注入调试日志
- 每个方法被调用时会打印日志
- 日志格式: [UNILAB] ClassName.method_name() called
- 查看日志: logging.getLogger("unilab.driver").setLevel(logging.DEBUG)
"""
from typing import Dict, Any, Optional
import logging
import warnings as _warnings
_warnings.filterwarnings("ignore", message=r"pkg_resources is deprecated as an API\..*")
import sys as _sys
import types as _types

# 兼容性：部分上游框架（例如 lantz）在 import-time 强制加载 Qt（PyQt4/PySide1），
# 在 headless/现代 Python 环境中通常不存在这些绑定，会导致导入直接失败。
# 这里预注入一个最小的 `lantz.utils.qt` shim，避免触发其 Qt loader。
if "lantz.utils.qt" not in _sys.modules:
    _qt_mod = _types.ModuleType("lantz.utils.qt")

    class _DummyQtCore:
        class QObject:
            def __init__(self, *_args, **_kwargs):
                # 兼容 QObject(parent=...) 等调用
                return None

        @staticmethod
        def Signal(*_args, **_kwargs):
            return object()

        @staticmethod
        def Slot(*_args, **_kwargs):
            def _decorator(fn):
                return fn

            return _decorator

    _qt_mod.QtCore = _DummyQtCore

    class _DummySuperQObject(_DummyQtCore.QObject):
        def __new__(cls, *_args, **_kwargs):
            return super().__new__(cls)

        def __init__(self, *_args, **_kwargs):
            # 允许 Driver(...) 传参，不触发 object.__init__ 的 TypeError
            return None

    _qt_mod.MetaQObject = type
    _qt_mod.SuperQObject = _DummySuperQObject
    _sys.modules["lantz.utils.qt"] = _qt_mod

# Unilab 驱动调试日志
_unilab_logger = logging.getLogger("unilab.driver.Racal1992")
_unilab_logger.setLevel(logging.DEBUG)
if not _unilab_logger.handlers:
    _handler = logging.StreamHandler()
    _handler.setFormatter(logging.Formatter("[%(asctime)s] %(message)s"))
    _unilab_logger.addHandler(_handler)

# Vendored repo packages: labtoolkit, labtoolkit, labtoolkit.FrequencyCounter.FrequencyCounter
import sys as _sys
from pathlib import Path as _Path
_vendor_root = _Path(__file__).resolve().parent / "_vendor"
if _vendor_root.exists() and str(_vendor_root) not in _sys.path:
    _sys.path.insert(0, str(_vendor_root))

from labtoolkit.Instrument import Instrument
from labtoolkit.FrequencyCounter.FrequencyCounter import FrequencyCounter


class Racal1992(Instrument, FrequencyCounter):
    """."""

    def __init__(self, *args, **kwargs):
        print("[UNILAB] Racal1992.__init__() called", flush=True)
        super().__init__(*args, **kwargs)
    def __post__(self):
        _unilab_logger.debug("[UNILAB] Racal1992.__post__() called")
        self.inst.timeout = 9000  # to much?

    '''
    IP preset
    FA Frequency source port a
    SRS10 digits 10
    SRS8 digits 8
    T0 continous trig
    T1 hold wating for trig
    T2 single trig query read
    '''

    @property
    def frequency(self):
        _unilab_logger.debug("[UNILAB] Racal1992.frequency() called")
        """."""
        return float(self.query('T2', delay=0.1)[3:])

    def preset(self):
        _unilab_logger.debug("[UNILAB] Racal1992.preset() called")
        """."""
        self.write('IP')

    def port(self, port):
        _unilab_logger.debug("[UNILAB] Racal1992.port() called")
        """."""
        self.write(f'F{port.upper()}')

    def remote(self):
        _unilab_logger.debug("[UNILAB] Racal1992.remote() called")
        """."""
        self.write_list(['SRS10', 'T1'], delay=0.1)

    def local(self):
        _unilab_logger.debug("[UNILAB] Racal1992.local() called")
        """."""
        self.write_list(['SRS8', 'T0'], delay=0.1)

# print(value)  # in Hz
# print(value / 1e6)  # in MHz
# print(round(value / 1e6, 14))  # in MHz
# print(round(value - 50e6, 8))  # in Hz error from nominal 50MHz

__all__ = ["Racal1992"]
