"""
Auto-generated by Build Agent
Original: wasatch/RealUSBDevice.py

[UNILAB DEBUG] 此驱动已注入调试日志
- 每个方法被调用时会打印日志
- 日志格式: [UNILAB] ClassName.method_name() called
- 查看日志: logging.getLogger("unilab.driver").setLevel(logging.DEBUG)
"""
from typing import Dict, Any, Optional
import logging
import warnings as _warnings
_warnings.filterwarnings("ignore", message=r"pkg_resources is deprecated as an API\..*")
import sys as _sys
import types as _types

# 兼容性：部分上游框架（例如 lantz）在 import-time 强制加载 Qt（PyQt4/PySide1），
# 在 headless/现代 Python 环境中通常不存在这些绑定，会导致导入直接失败。
# 这里预注入一个最小的 `lantz.utils.qt` shim，避免触发其 Qt loader。
if "lantz.utils.qt" not in _sys.modules:
    _qt_mod = _types.ModuleType("lantz.utils.qt")

    class _DummyQtCore:
        class QObject:
            def __init__(self, *_args, **_kwargs):
                # 兼容 QObject(parent=...) 等调用
                return None

        @staticmethod
        def Signal(*_args, **_kwargs):
            return object()

        @staticmethod
        def Slot(*_args, **_kwargs):
            def _decorator(fn):
                return fn

            return _decorator

    _qt_mod.QtCore = _DummyQtCore

    class _DummySuperQObject(_DummyQtCore.QObject):
        def __new__(cls, *_args, **_kwargs):
            return super().__new__(cls)

        def __init__(self, *_args, **_kwargs):
            # 允许 Driver(...) 传参，不触发 object.__init__ 的 TypeError
            return None

    _qt_mod.MetaQObject = type
    _qt_mod.SuperQObject = _DummySuperQObject
    _sys.modules["lantz.utils.qt"] = _qt_mod

# Unilab 驱动调试日志
_unilab_logger = logging.getLogger("unilab.driver.RealUSBDevice")
_unilab_logger.setLevel(logging.DEBUG)
if not _unilab_logger.handlers:
    _handler = logging.StreamHandler()
    _handler.setFormatter(logging.Formatter("[%(asctime)s] %(message)s"))
    _unilab_logger.addHandler(_handler)

# Vendored repo packages: wasatch, wasatch
import sys as _sys
from pathlib import Path as _Path
_vendor_root = _Path(__file__).resolve().parent / "_vendor"
if _vendor_root.exists() and str(_vendor_root) not in _sys.path:
    _sys.path.insert(0, str(_vendor_root))

import usb
import logging
import usb.backend.libusb0 as libusb0
from wasatch.AbstractUSBDevice import AbstractUSBDevice

log = logging.getLogger(__name__)

class RealUSBDevice(AbstractUSBDevice):

    def __init__(self,device_id):
        print("[UNILAB] RealUSBDevice.__init__() called", flush=True)
        self.device_id = device_id
        self.vid = self.device_id.vid
        self.pid = self.device_id.pid
        self.bus = self.device_id.bus
        self.address = self.device_id.address

    def find(self, *args, **kwargs):
        _unilab_logger.debug("[UNILAB] RealUSBDevice.find() called")
        return usb.core.find(*args, **kwargs, backend=libusb0.get_backend())

    def set_configuration(self, device):
        _unilab_logger.debug("[UNILAB] RealUSBDevice.set_configuration() called")
        device.set_configuration()

    def reset(self, dev):
        _unilab_logger.debug("[UNILAB] RealUSBDevice.reset() called")
        dev.reset()

    def claim_interface(self, *args, **kwargs):
        _unilab_logger.debug("[UNILAB] RealUSBDevice.claim_interface() called")
        return usb.util.claim_interface(*args, **kwargs)

    def release_interface(self, *args, **kwargs):
        _unilab_logger.debug("[UNILAB] RealUSBDevice.release_interface() called")
        return usb.util.release_interface(*args, **kwargs)

    def ctrl_transfer(self, *args):
        _unilab_logger.debug("[UNILAB] RealUSBDevice.ctrl_transfer() called")
        device = args[0]
        ctrl_args = args[1:]
        return device.ctrl_transfer(*ctrl_args)

    def read(self, *args, **kwargs):
        _unilab_logger.debug("[UNILAB] RealUSBDevice.read() called")
        device =  args[0]
        read_args = args[1:]
        return device.read(*read_args, **kwargs)

    def send_code(self):
        _unilab_logger.debug("[UNILAB] RealUSBDevice.send_code() called")
        pass

    def to_dict(self):
        _unilab_logger.debug("[UNILAB] RealUSBDevice.to_dict() called")
        return str(self)

    def __str__(self):
        _unilab_logger.debug("[UNILAB] RealUSBDevice.__str__() called")
        return "<RealUSBDevice 0x%04x:0x%04x:%d:%d>" % (self.vid, self.pid, self.bus, self.address)

    def __hash__(self):
        _unilab_logger.debug("[UNILAB] RealUSBDevice.__hash__() called")
        return hash(str(self))

    def __repr__(self):
        _unilab_logger.debug("[UNILAB] RealUSBDevice.__repr__() called")
        return str(self)

    def __eq__(self, other):
        _unilab_logger.debug("[UNILAB] RealUSBDevice.__eq__() called")
        return str(self) == str(other)

    def __ne__(self, other):
        _unilab_logger.debug("[UNILAB] RealUSBDevice.__ne__() called")
        return str(self) != str(other)

    def __lt__(self, other):
        _unilab_logger.debug("[UNILAB] RealUSBDevice.__lt__() called")
        return str(self) < str(other)

__all__ = ["RealUSBDevice"]
