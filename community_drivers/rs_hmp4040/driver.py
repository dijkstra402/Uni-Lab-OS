"""
Auto-generated by Build Agent
Original: basil/HL/rs_hmp4040.py

[UNILAB DEBUG] 此驱动已注入调试日志
- 每个方法被调用时会打印日志
- 日志格式: [UNILAB] ClassName.method_name() called
- 查看日志: logging.getLogger("unilab.driver").setLevel(logging.DEBUG)
"""
from typing import Dict, Any, Optional
import logging
import warnings as _warnings
_warnings.filterwarnings("ignore", message=r"pkg_resources is deprecated as an API\..*")
import sys as _sys
import types as _types

# 兼容性：部分上游框架（例如 lantz）在 import-time 强制加载 Qt（PyQt4/PySide1），
# 在 headless/现代 Python 环境中通常不存在这些绑定，会导致导入直接失败。
# 这里预注入一个最小的 `lantz.utils.qt` shim，避免触发其 Qt loader。
if "lantz.utils.qt" not in _sys.modules:
    _qt_mod = _types.ModuleType("lantz.utils.qt")

    class _DummyQtCore:
        class QObject:
            def __init__(self, *_args, **_kwargs):
                # 兼容 QObject(parent=...) 等调用
                return None

        @staticmethod
        def Signal(*_args, **_kwargs):
            return object()

        @staticmethod
        def Slot(*_args, **_kwargs):
            def _decorator(fn):
                return fn

            return _decorator

    _qt_mod.QtCore = _DummyQtCore

    class _DummySuperQObject(_DummyQtCore.QObject):
        def __new__(cls, *_args, **_kwargs):
            return super().__new__(cls)

        def __init__(self, *_args, **_kwargs):
            # 允许 Driver(...) 传参，不触发 object.__init__ 的 TypeError
            return None

    _qt_mod.MetaQObject = type
    _qt_mod.SuperQObject = _DummySuperQObject
    _sys.modules["lantz.utils.qt"] = _qt_mod

# Unilab 驱动调试日志
_unilab_logger = logging.getLogger("unilab.driver.rs_HMP4040")
_unilab_logger.setLevel(logging.DEBUG)
if not _unilab_logger.handlers:
    _handler = logging.StreamHandler()
    _handler.setFormatter(logging.Formatter("[%(asctime)s] %(message)s"))
    _unilab_logger.addHandler(_handler)

# Vendored repo packages: basil, basil
import sys as _sys
from pathlib import Path as _Path
_vendor_root = _Path(__file__).resolve().parent / "_vendor"
if _vendor_root.exists() and str(_vendor_root) not in _sys.path:
    _sys.path.insert(0, str(_vendor_root))

import logging
import time

from basil.HL.RegisterHardwareLayer import HardwareLayer


logger = logging.getLogger(__name__)


class rs_HMP4040(HardwareLayer):
    '''
    HL for the RS HMP4040
    !! Analog to tti ql355tp for compatibility with bdaq53
    '''

    def __init__(self, intf, conf):
        print("[UNILAB] rs_HMP4040.__init__() called", flush=True)
        super(rs_HMP4040, self).__init__(intf, conf)

    def init(self):
        _unilab_logger.debug("[UNILAB] rs_HMP4040.init() called")
        super(rs_HMP4040, self).init()

    def reinit(self):
        _unilab_logger.debug("[UNILAB] rs_HMP4040.reinit() called")
        self._intf.init()

    def write(self, command):
        _unilab_logger.debug("[UNILAB] rs_HMP4040.write() called")
        self._intf.write(command)
        time.sleep(0.1)

    def ask(self, command):
        _unilab_logger.debug("[UNILAB] rs_HMP4040.ask() called")
        self.write(command)
        return self.read()

    def read(self):
        _unilab_logger.debug("[UNILAB] rs_HMP4040.read() called")
        ret = self._intf.read()
        time.sleep(0.1)
        return ret

    def set_channel(self, channel):
        _unilab_logger.debug("[UNILAB] rs_HMP4040.set_channel() called")
        """ channel: 1=OP1, 2=OP2, 3=OP3, 4=OP4"""
        cmd = "INST:NSEL %d" % channel
        self.write(cmd)

    def get_channel(self):
        _unilab_logger.debug("[UNILAB] rs_HMP4040.get_channel() called")
        ret = self.ask("INST:NSEL?")
        return ret

    def set_enable(self, on, channel=1):
        _unilab_logger.debug("[UNILAB] rs_HMP4040.set_enable() called")
        """ channel: 1=OP1, 2=OP2, 3=OP3, 4=OP4, ALL=all channels"""
        if channel == "ALL":
            for i in [1, 2, 3, 4]:
                self.set_enable(on, channel=i)
        else:
            self.set_channel(channel)

            if on:
                cmd = "OUTP ON"
            else:
                cmd = "OUTP OFF"

            self.write(cmd)
        time.sleep(1)

    def get_name(self):
        _unilab_logger.debug("[UNILAB] rs_HMP4040.get_name() called")
        return self.ask("*IDN?")

    def get_current(self, channel):
        _unilab_logger.debug("[UNILAB] rs_HMP4040.get_current() called")
        """ channel: 1=OP1, 2=OP2, 3=OP3, 4=OP4"""
        self.set_channel(channel)
        ret = self.ask("MEAS:CURR?")

        return float(ret)

    def get_voltage(self, channel):
        _unilab_logger.debug("[UNILAB] rs_HMP4040.get_voltage() called")
        """ channel: 1=OP1, 2=OP2, 3=OP3, 4=OP4"""
        self.set_channel(channel)
        ret = self.ask("MEAS:VOLT?")

        return float(ret)

    def get_set_voltage(self, channel):
        _unilab_logger.debug("[UNILAB] rs_HMP4040.get_set_voltage() called")
        """ channel: 1=OP1, 2=OP2, 3=OP3, 4=OP4"""
        self.set_channel(channel)
        ret = self.ask("VOLT?")

        return float(ret)

    def get_current_limit(self, channel):
        _unilab_logger.debug("[UNILAB] rs_HMP4040.get_current_limit() called")
        """ channel: 1=OP1, 2=OP2, 3=OP3, 4=OP4"""
        self.set_channel(channel)
        ret = self.ask("CURR?")

        return float(ret)

    def set_voltage(self, value, channel=1):
        _unilab_logger.debug("[UNILAB] rs_HMP4040.set_voltage() called")
        """ channel: 1=OP1, 2=OP2, 3=OP3, 4=OP4"""
        self.set_channel(channel)
        cmd = "VOLT %f" % value
        self.write(cmd)

    def set_current_limit(self, value, channel=1):
        _unilab_logger.debug("[UNILAB] rs_HMP4040.set_current_limit() called")
        """ channel: 1=OP1, 2=OP2, 3=OP3, 4=OP4"""
        self.set_channel(channel)
        cmd = "CURR %f" % value
        self.write(cmd)

    def reset_trip(self):
        _unilab_logger.debug("[UNILAB] rs_HMP4040.reset_trip() called")
        cmd = "VOLT:PROT:CLE"
        self.write(cmd)
        time.sleep(1)

__all__ = ["rs_HMP4040"]
