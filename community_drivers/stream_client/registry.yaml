stream_client:
  class:
    module: unilabos.devices.stream_client.stream_client:StreamClient
    type: python
    status_types: {}
    action_value_mappings:
      read:
        action_name: read
        args_info:
          nelems:
            type: str
          data_size:
            type: str
      get_blocks:
        action_name: get_blocks
        args_info:
          nelems:
            type: str
          data_size:
            type: str
      st_monitor:
        action_name: st_monitor
      get_state:
        action_name: get_state
      get_pre:
        action_name: get_pre
      get_post:
        action_name: get_post
      get_total:
        action_name: get_total
      get_elapsed:
        action_name: get_elapsed
      wait_event:
        action_name: wait_event
        args_info:
          ev:
            type: str
          descr:
            type: str
      wait_armed:
        action_name: wait_armed
      wait_stopped:
        action_name: wait_stopped
      wait_state_changed:
        action_name: wait_state_changed
      init_site_client:
        action_name: init_site_client
        args_info:
          site:
            type: str
      make_sa_sd_aliases:
        action_name: make_sa_sd_aliases
      get_sys_info:
        action_name: get_sys_info
      close:
        action_name: close
      state:
        action_name: state
      post_samples:
        action_name: post_samples
      pre_samples:
        action_name: pre_samples
      elapsed_samples:
        action_name: elapsed_samples
      demux_status:
        action_name: demux_status
      samples:
        action_name: samples
      get_aggregator_sites:
        action_name: get_aggregator_sites
      get_aggregator_svc_list:
        action_name: get_aggregator_svc_list
      fetch_all_calibration:
        action_name: fetch_all_calibration
      scale_raw:
        action_name: scale_raw
        args_info:
          raw:
            type: str
          volts:
            type: str
      chan2volts:
        action_name: chan2volts
        args_info:
          chan:
            type: str
          raw:
            type: str
      read_chan:
        action_name: read_chan
        args_info:
          chan:
            type: str
          nsam:
            type: str
          data_size:
            type: str
      read_decims:
        action_name: read_decims
        args_info:
          nsam:
            type: str
      nchan:
        action_name: nchan
      data_size:
        action_name: data_size
        args_info:
          site:
            type: str
      uut_demux_enabled:
        action_name: uut_demux_enabled
      read_transient_timebase:
        action_name: read_transient_timebase
        args_info:
          nsamples:
            type: str
          pre:
            type: str
      load_segments:
        action_name: load_segments
        args_info:
          segs:
            type: str
      show_segments:
        action_name: show_segments
      clear_counters:
        action_name: clear_counters
      set_sync_routing_master:
        action_name: set_sync_routing_master
        args_info:
          clk_dx:
            type: str
          trg_dx:
            type: str
      set_sync_routing_slave:
        action_name: set_sync_routing_slave
      set_sync_routing:
        action_name: set_sync_routing
        args_info:
          role:
            type: str
      set_mb_clk:
        action_name: set_mb_clk
        args_info:
          hz:
            type: str
          src:
            type: str
          fin:
            type: str
      load_stl:
        action_name: load_stl
        args_info:
          stl:
            type: str
          port:
            type: str
          trace:
            type: str
          wait_eof:
            type: str
          wait_eol:
            type: str
      load_gpg:
        action_name: load_gpg
        args_info:
          stl:
            type: str
          trace:
            type: str
      load_dpg:
        action_name: load_dpg
        args_info:
          stl:
            type: str
          trace:
            type: str
      load_wrpg:
        action_name: load_wrpg
        args_info:
          stl:
            type: str
          trace:
            type: str
      load_dio482pg:
        action_name: load_dio482pg
        args_info:
          site:
            type: str
          stl:
            type: str
          trace:
            type: str
      set_DO:
        action_name: set_DO
        args_info:
          site:
            type: str
          dox:
            type: str
          value:
            type: str
      load_awg:
        action_name: load_awg
        args_info:
          data:
            type: str
          autorearm:
            type: str
          continuous:
            type: str
          repeats:
            type: str
          port:
            type: str
      set_segment:
        action_name: set_segment
        args_info:
          segment:
            type: str
      run_service:
        action_name: run_service
        args_info:
          port:
            type: str
          eof:
            type: str
          prompt:
            type: str
  description: Stream Client是一款实验执行&合成设备，主要功能包括使能、停止、关闭、初始化、加载、参数配置。支持SVC, Signal, Slot等操作。
  category:
  - 实验执行&合成设备
  init_param_schema:
    config:
      type: object
      properties:
        addr:
          type: string
          default: null
  name: Stream
  manufacturer: Open Source
  icon: ascii_stl__writer.webp
  tags:
  - 实验执行&合成设备
  device_params:
    支持操作数: '99'
    读取操作: read、get_blocks、get_state、get_pre、get_post
    控制操作: wait_stopped、uut_demux_enabled、set_sync_routing_master、set_sync_routing_slave、set_sync_routing
    控制能力: 位置控制、多通道、电压控制、电流控制
