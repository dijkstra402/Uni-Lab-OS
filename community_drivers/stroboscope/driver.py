"""
Auto-generated by Build Agent
Original: pycqed/instrument_drivers/physical_instruments/Stroboscope/Stroboscope.py

[UNILAB DEBUG] 此驱动已注入调试日志
- 每个方法被调用时会打印日志
- 日志格式: [UNILAB] ClassName.method_name() called
- 查看日志: logging.getLogger("unilab.driver").setLevel(logging.DEBUG)
"""
from typing import Dict, Any, Optional
import logging
import warnings as _warnings
_warnings.filterwarnings("ignore", message=r"pkg_resources is deprecated as an API\..*")
import sys as _sys
import types as _types

# 兼容性：部分上游框架（例如 lantz）在 import-time 强制加载 Qt（PyQt4/PySide1），
# 在 headless/现代 Python 环境中通常不存在这些绑定，会导致导入直接失败。
# 这里预注入一个最小的 `lantz.utils.qt` shim，避免触发其 Qt loader。
if "lantz.utils.qt" not in _sys.modules:
    _qt_mod = _types.ModuleType("lantz.utils.qt")

    class _DummyQtCore:
        class QObject:
            def __init__(self, *_args, **_kwargs):
                # 兼容 QObject(parent=...) 等调用
                return None

        @staticmethod
        def Signal(*_args, **_kwargs):
            return object()

        @staticmethod
        def Slot(*_args, **_kwargs):
            def _decorator(fn):
                return fn

            return _decorator

    _qt_mod.QtCore = _DummyQtCore

    class _DummySuperQObject(_DummyQtCore.QObject):
        def __new__(cls, *_args, **_kwargs):
            return super().__new__(cls)

        def __init__(self, *_args, **_kwargs):
            # 允许 Driver(...) 传参，不触发 object.__init__ 的 TypeError
            return None

    _qt_mod.MetaQObject = type
    _qt_mod.SuperQObject = _DummySuperQObject
    _sys.modules["lantz.utils.qt"] = _qt_mod

# Unilab 驱动调试日志
_unilab_logger = logging.getLogger("unilab.driver.Stroboscope")
_unilab_logger.setLevel(logging.DEBUG)
if not _unilab_logger.handlers:
    _handler = logging.StreamHandler()
    _handler.setFormatter(logging.Formatter("[%(asctime)s] %(message)s"))
    _unilab_logger.addHandler(_handler)

# Vendored repo packages: pycqed
import sys as _sys
from pathlib import Path as _Path
_vendor_root = _Path(__file__).resolve().parent / "_vendor"
if _vendor_root.exists() and str(_vendor_root) not in _sys.path:
    _sys.path.insert(0, str(_vendor_root))

# Stroboscope for locking the measurement to the pulse tube and directing the
# triggers to the measurement setup
#
# Written April 2018:
# Thijs Stavenga <thijsstavenga@msn.com>
#


from qcodes import VisaInstrument
from qcodes.utils.validators import Strings
from qcodes import validators as vals
# import time


class Stroboscope(VisaInstrument):
    '''
    This is the driver for the Stroboscope

    Usage:
    Initialize with
    <name> = instruments.create('<name>', 'Keithley_2200',
        address='<address>',
        reset=<bool>,
        change_display=<bool>,
        change_autozero=<bool>)
    '''

    # def __init__(self, name, address, reset=False,
    #         change_display=True, change_autozero=True):
    def __init__(self, name, address, **kwargs):
        print("[UNILAB] Stroboscope.__init__() called", flush=True)
        '''
        Initializes the Keithley_2200, and communicates with the wrapper.

        Input:
            name (string)           : name of the instrument
            address (string)        : COM port
        '''

        super().__init__(name, address, **kwargs)

        # Add some global constants
        self.MAX_DUTY = 698 ## milliseconds
        self.MAX_PHASE = 698 ## milliseconds
        self._address = address
        self.visa_handle.baud_rate = 115200
        self.visa_handle.write_termination = '\r'
        self.visa_handle.read_termination = '\r'

        self.add_parameter('phase',
            get_cmd='PHASE?',
            get_parser=int,
            set_cmd='PHASE {:d}',
            vals=vals.Numbers(min_value=0,
                              max_value=self.MAX_PHASE),
            unit='ms')
        self.add_parameter('duty_cycle',
            get_cmd='DUTY?',
            get_parser=int,
            set_cmd='DUTY {:d}',
            vals=vals.Numbers(min_value=0,
                              max_value=self.MAX_DUTY),
            unit='ms')
        self.add_parameter('locked',
            get_cmd='LOCK?',
            get_parser=lambda s: bool(int(s)),
            label='Trigger locked to pulse tube')

        # time.sleep(3)
        # self.get_all()

# --------------------------------------
#           functions
# --------------------------------------

    def get_all(self):
        _unilab_logger.debug("[UNILAB] Stroboscope.get_all() called")
        '''
        Reads all relevant parameters from instrument

        Input:
            None

        Output:
            None
        '''

        self.phase()
        self.duty_cycle()
        self.locked()


    def get_info(self):
        _unilab_logger.debug("[UNILAB] Stroboscope.get_info() called")
        '''
        Get the device descriptor.
        should be: Keithley Instruments, 2200-20-5, 1359014, 1.26-1.23
        Input:
            None
        Output:
            String: Device descriptor
        '''
        ans = self._visainstrument.ask('*IDN?')
        return ans

