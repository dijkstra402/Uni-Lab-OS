"""
Auto-generated by Build Agent
Original: lantz/drivers/legacy/network.py

[UNILAB DEBUG] 此驱动已注入调试日志
- 每个方法被调用时会打印日志
- 日志格式: [UNILAB] ClassName.method_name() called
- 查看日志: logging.getLogger("unilab.driver").setLevel(logging.DEBUG)
"""
from typing import Dict, Any, Optional
import logging
import warnings as _warnings
_warnings.filterwarnings("ignore", message=r"pkg_resources is deprecated as an API\..*")
import sys as _sys
import types as _types

# 兼容性：部分上游框架（例如 lantz）在 import-time 强制加载 Qt（PyQt4/PySide1），
# 在 headless/现代 Python 环境中通常不存在这些绑定，会导致导入直接失败。
# 这里预注入一个最小的 `lantz.utils.qt` shim，避免触发其 Qt loader。
if "lantz.utils.qt" not in _sys.modules:
    _qt_mod = _types.ModuleType("lantz.utils.qt")

    class _DummyQtCore:
        class QObject:
            def __init__(self, *_args, **_kwargs):
                # 兼容 QObject(parent=...) 等调用
                return None

        @staticmethod
        def Signal(*_args, **_kwargs):
            return object()

        @staticmethod
        def Slot(*_args, **_kwargs):
            def _decorator(fn):
                return fn

            return _decorator

    _qt_mod.QtCore = _DummyQtCore

    class _DummySuperQObject(_DummyQtCore.QObject):
        def __new__(cls, *_args, **_kwargs):
            return super().__new__(cls)

        def __init__(self, *_args, **_kwargs):
            # 允许 Driver(...) 传参，不触发 object.__init__ 的 TypeError
            return None

    _qt_mod.MetaQObject = type
    _qt_mod.SuperQObject = _DummySuperQObject
    _sys.modules["lantz.utils.qt"] = _qt_mod

# Unilab 驱动调试日志
_unilab_logger = logging.getLogger("unilab.driver.TCPRawDriver")
_unilab_logger.setLevel(logging.DEBUG)
if not _unilab_logger.handlers:
    _handler = logging.StreamHandler()
    _handler.setFormatter(logging.Formatter("[%(asctime)s] %(message)s"))
    _unilab_logger.addHandler(_handler)

# Vendored repo packages: lantz, lantz
import sys as _sys
from pathlib import Path as _Path
_vendor_root = _Path(__file__).resolve().parent / "_vendor"
if _vendor_root.exists() and str(_vendor_root) not in _sys.path:
    _sys.path.insert(0, str(_vendor_root))

# -*- coding: utf-8 -*-
"""
    lantz.drivers.legacy.network
    ~~~~~~~~~~~~~~~~~~~~~~~~~~~~

    Implements a base class for drivers that communicate with instruments via TCP.

    :copyright: 2015 by Lantz Authors, see AUTHORS for more details.
    :license: BSD, see LICENSE for more details.
"""

import socket

from lantz import Driver
from lantz.drivers.legacy.textual import TextualMixin
from lantz.errors import LantzTimeoutError


class LantzSocketTimeoutError(socket.timeout, LantzTimeoutError):
    pass


class TCPRawDriver(Driver):
    """Base class for drivers that communicate with instruments via TCP.

    :param host: Address of the network resource
    :param port: Port number
    """

    def __init__(self, *args, **kwargs):
        print("[UNILAB] TCPRawDriver.__init__() called", flush=True)
        super().__init__(*args, **kwargs)
    RECV_TERMINATION = '\n'
    SEND_TERMINATION = '\n'

    RECV_CHUNK = 1024

    def __init__(self, host='localhost', port=9997, *args, **kwargs):
        print("[UNILAB] TCPRawDriver.__init__() called", flush=True)
        super().__init__(*args, **kwargs)
        self.socket = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
        self.host_port = (host, port)

    def raw_send(self, data):
        _unilab_logger.debug("[UNILAB] TCPRawDriver.raw_send() called")
        """Send raw bytes to the instrument.

        :param data: bytes to be sent to the instrument.
        :param data: bytes.
        """
        try:
            self.socket.send(data)
        except socket.timeout as e:
            raise LantzSocketTimeoutError(str(e))

    def raw_recv(self, size):
        _unilab_logger.debug("[UNILAB] TCPRawDriver.raw_recv() called")
        """Receive raw bytes to the instrument.

        :param size: number of bytes to receive.
        :return: received bytes.
        :return type: bytes.
        """
        try:
            return self.socket.recv(size)
        except socket.timeout as e:
            raise LantzSocketTimeoutError(str(e))

    def initialize(self):
        _unilab_logger.debug("[UNILAB] TCPRawDriver.initialize() called")
        self.log_debug('Opening port {}', self.host_port)
        return self.socket.connect(self.host_port)

    def finalize(self):
        _unilab_logger.debug("[UNILAB] TCPRawDriver.finalize() called")
        self.log_debug('Closing port {}', self.host_port)
        return self.socket.close()

    def is_open(self):
        _unilab_logger.debug("[UNILAB] TCPRawDriver.is_open() called")
        return self.socket.isOpen()


class TCPDriver(TCPRawDriver, TextualMixin):
    """Base class for drivers that communicate with instruments via TCP.

    :param host: Address of the network resource
    :param port: Port number
    """

    RECV_TERMINATION = '\n'
    SEND_TERMINATION = '\n'

    RECV_CHUNK = 1024

