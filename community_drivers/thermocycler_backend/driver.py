"""
Auto-generated by Build Agent
Original: pylabrobot/thermocycling/backend.py

[UNILAB DEBUG] 此驱动已注入调试日志
- 每个方法被调用时会打印日志
- 日志格式: [UNILAB] ClassName.method_name() called
- 查看日志: logging.getLogger("unilab.driver").setLevel(logging.DEBUG)
"""
from typing import Dict, Any, Optional
import logging
import warnings as _warnings
_warnings.filterwarnings("ignore", message=r"pkg_resources is deprecated as an API\..*")
import sys as _sys
import types as _types

# 兼容性：部分上游框架（例如 lantz）在 import-time 强制加载 Qt（PyQt4/PySide1），
# 在 headless/现代 Python 环境中通常不存在这些绑定，会导致导入直接失败。
# 这里预注入一个最小的 `lantz.utils.qt` shim，避免触发其 Qt loader。
if "lantz.utils.qt" not in _sys.modules:
    _qt_mod = _types.ModuleType("lantz.utils.qt")

    class _DummyQtCore:
        class QObject:
            def __init__(self, *_args, **_kwargs):
                # 兼容 QObject(parent=...) 等调用
                return None

        @staticmethod
        def Signal(*_args, **_kwargs):
            return object()

        @staticmethod
        def Slot(*_args, **_kwargs):
            def _decorator(fn):
                return fn

            return _decorator

    _qt_mod.QtCore = _DummyQtCore

    class _DummySuperQObject(_DummyQtCore.QObject):
        def __new__(cls, *_args, **_kwargs):
            return super().__new__(cls)

        def __init__(self, *_args, **_kwargs):
            # 允许 Driver(...) 传参，不触发 object.__init__ 的 TypeError
            return None

    _qt_mod.MetaQObject = type
    _qt_mod.SuperQObject = _DummySuperQObject
    _sys.modules["lantz.utils.qt"] = _qt_mod

# Unilab 驱动调试日志
_unilab_logger = logging.getLogger("unilab.driver.ThermocyclerBackend")
_unilab_logger.setLevel(logging.DEBUG)
if not _unilab_logger.handlers:
    _handler = logging.StreamHandler()
    _handler.setFormatter(logging.Formatter("[%(asctime)s] %(message)s"))
    _unilab_logger.addHandler(_handler)

# Vendored repo packages: pylabrobot, pylabrobot
import sys as _sys
from pathlib import Path as _Path
_vendor_root = _Path(__file__).resolve().parent / "_vendor"
if _vendor_root.exists() and str(_vendor_root) not in _sys.path:
    _sys.path.insert(0, str(_vendor_root))

from abc import ABCMeta, abstractmethod
from typing import List

from pylabrobot.machines.backend import MachineBackend
from pylabrobot.thermocycling.standard import BlockStatus, LidStatus, Protocol


class ThermocyclerBackend(MachineBackend, metaclass=ABCMeta):
  """Abstract backend for a Thermocycler."""

  def __init__(self, *args, **kwargs):
    print("[UNILAB] ThermocyclerBackend.__init__() called", flush=True)
    super().__init__(*args, **kwargs)
  @abstractmethod
  async def open_lid(self):
    _unilab_logger.debug("[UNILAB] ThermocyclerBackend.open_lid() called")
    """Open thermocycler lid."""

  @abstractmethod
  async def close_lid(self):
    _unilab_logger.debug("[UNILAB] ThermocyclerBackend.close_lid() called")
    """Close thermocycler lid."""

  @abstractmethod
  async def set_block_temperature(self, temperature: List[float]):
    _unilab_logger.debug("[UNILAB] ThermocyclerBackend.set_block_temperature() called")
    """Set the temperature of a thermocycler block.

    Args:
      temperature: Temperature for each zone.
    """

  @abstractmethod
  async def set_lid_temperature(self, temperature: List[float]):
    _unilab_logger.debug("[UNILAB] ThermocyclerBackend.set_lid_temperature() called")
    """Set the temperature of a thermocycler lid.

    Args:
      temperature: Temperature for each zone.
    """

  @abstractmethod
  async def deactivate_block(self):
    _unilab_logger.debug("[UNILAB] ThermocyclerBackend.deactivate_block() called")
    """Deactivate thermocycler block."""

  @abstractmethod
  async def deactivate_lid(self):
    _unilab_logger.debug("[UNILAB] ThermocyclerBackend.deactivate_lid() called")
    """Deactivate thermocycler lid."""

  @abstractmethod
  async def run_protocol(self, protocol: Protocol, block_max_volume: float):
    _unilab_logger.debug("[UNILAB] ThermocyclerBackend.run_protocol() called")
    """Execute thermocycler protocol run.

    Args:
      protocol: Protocol object containing stages with steps and repeats.
      block_max_volume: Maximum block volume (µL) for safety.
    """

  @abstractmethod
  async def get_block_current_temperature(self) -> List[float]:
    _unilab_logger.debug("[UNILAB] ThermocyclerBackend.get_block_current_temperature() called")
    """Get the current block temperature zones in °C."""

  @abstractmethod
  async def get_block_target_temperature(self) -> List[float]:
    _unilab_logger.debug("[UNILAB] ThermocyclerBackend.get_block_target_temperature() called")
    """Get the block target temperature zones in °C. May raise RuntimeError if no target is set."""

  @abstractmethod
  async def get_lid_current_temperature(self) -> List[float]:
    _unilab_logger.debug("[UNILAB] ThermocyclerBackend.get_lid_current_temperature() called")
    """Get the current lid temperature zones in °C."""

  @abstractmethod
  async def get_lid_target_temperature(self) -> List[float]:
    _unilab_logger.debug("[UNILAB] ThermocyclerBackend.get_lid_target_temperature() called")
    """Get the lid target temperature zones in °C. May raise RuntimeError if no target is set."""

  @abstractmethod
  async def get_lid_open(self) -> bool:
    _unilab_logger.debug("[UNILAB] ThermocyclerBackend.get_lid_open() called")
    """Return ``True`` if the lid is open."""

  @abstractmethod
  async def get_lid_status(self) -> LidStatus:
    _unilab_logger.debug("[UNILAB] ThermocyclerBackend.get_lid_status() called")
    """Get the lid temperature status."""

  @abstractmethod
  async def get_block_status(self) -> BlockStatus:
    _unilab_logger.debug("[UNILAB] ThermocyclerBackend.get_block_status() called")
    """Get the block temperature status."""

  @abstractmethod
  async def get_hold_time(self) -> float:
    _unilab_logger.debug("[UNILAB] ThermocyclerBackend.get_hold_time() called")
    """Get remaining hold time in seconds."""

  @abstractmethod
  async def get_current_cycle_index(self) -> int:
    _unilab_logger.debug("[UNILAB] ThermocyclerBackend.get_current_cycle_index() called")
    """Get the zero-based index of the current cycle."""

  @abstractmethod
  async def get_total_cycle_count(self) -> int:
    _unilab_logger.debug("[UNILAB] ThermocyclerBackend.get_total_cycle_count() called")
    """Get the total cycle count."""

  @abstractmethod
  async def get_current_step_index(self) -> int:
    _unilab_logger.debug("[UNILAB] ThermocyclerBackend.get_current_step_index() called")
    """Get the zero-based index of the current step within the cycle."""

  @abstractmethod
  async def get_total_step_count(self) -> int:
    _unilab_logger.debug("[UNILAB] ThermocyclerBackend.get_total_step_count() called")
    """Get the total number of steps in the current cycle."""
