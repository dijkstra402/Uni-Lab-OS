"""
Auto-generated by Build Agent
Original: acq4/drivers/ThorlabsDC4100/mock.py

[UNILAB DEBUG] 此驱动已注入调试日志
- 每个方法被调用时会打印日志
- 日志格式: [UNILAB] ClassName.method_name() called
- 查看日志: logging.getLogger("unilab.driver").setLevel(logging.DEBUG)
"""
from typing import Dict, Any, Optional
import logging
import warnings as _warnings
_warnings.filterwarnings("ignore", message=r"pkg_resources is deprecated as an API\..*")
import sys as _sys
import types as _types

# 兼容性：部分上游框架（例如 lantz）在 import-time 强制加载 Qt（PyQt4/PySide1），
# 在 headless/现代 Python 环境中通常不存在这些绑定，会导致导入直接失败。
# 这里预注入一个最小的 `lantz.utils.qt` shim，避免触发其 Qt loader。
if "lantz.utils.qt" not in _sys.modules:
    _qt_mod = _types.ModuleType("lantz.utils.qt")

    class _DummyQtCore:
        class QObject:
            def __init__(self, *_args, **_kwargs):
                # 兼容 QObject(parent=...) 等调用
                return None

        @staticmethod
        def Signal(*_args, **_kwargs):
            return object()

        @staticmethod
        def Slot(*_args, **_kwargs):
            def _decorator(fn):
                return fn

            return _decorator

    _qt_mod.QtCore = _DummyQtCore

    class _DummySuperQObject(_DummyQtCore.QObject):
        def __new__(cls, *_args, **_kwargs):
            return super().__new__(cls)

        def __init__(self, *_args, **_kwargs):
            # 允许 Driver(...) 传参，不触发 object.__init__ 的 TypeError
            return None

    _qt_mod.MetaQObject = type
    _qt_mod.SuperQObject = _DummySuperQObject
    _sys.modules["lantz.utils.qt"] = _qt_mod

# Unilab 驱动调试日志
_unilab_logger = logging.getLogger("unilab.driver.ThorlabsDC4100")
_unilab_logger.setLevel(logging.DEBUG)
if not _unilab_logger.handlers:
    _handler = logging.StreamHandler()
    _handler.setFormatter(logging.Formatter("[%(asctime)s] %(message)s"))
    _unilab_logger.addHandler(_handler)

# Vendored repo packages: acq4
import sys as _sys
from pathlib import Path as _Path
_vendor_root = _Path(__file__).resolve().parent / "_vendor"
if _vendor_root.exists() and str(_vendor_root) not in _sys.path:
    _sys.path.insert(0, str(_vendor_root))

import logging
import os
import serial as Serial
from serial import SerialException


COMMANDS = {
    "set_brightness": "BP {} {}",
    "get_brightness": "BP? {}",
    "set_led_channel_state": "O {} {}",
    "led_on": "O {} 1",
    "led_off": "O {} 0",
    "return_on_off": "O? {}",
    "lock_led": "A {} {}",
    "register_status": "R?",
    "serial_number": "S?",  
    "firmware": "V?",
    "manufacturer": "H?",
    "error_status": "E?"
    }

class ThorlabsDC4100:
    def __init__(self,port,baudrate,timeout):
        print("[UNILAB] ThorlabsDC4100.__init__() called", flush=True)
        self.port = port
        self.baudrate = baudrate
        self.timeout = timeout
        self.dev = None
        self.escape = '\n\n'
        self.read_buffer = []
        self.channel_state = [ 0, 0, 0, 0]
    
    def connect_device(self):
        _unilab_logger.debug("[UNILAB] ThorlabsDC4100.connect_device() called")
        try:
            print('Mock ThorlabsDC4100')
            print('Connected port={}, baudrate={}, timeout={} '.format(self.port, self.baudrate, self.timeout ) )
            self.dev = 1
        except SerialException:
            logging.error("Device connection could not be established")
            
    def set_led_channel_state(self, channel, state):
        _unilab_logger.debug("[UNILAB] ThorlabsDC4100.set_led_channel_state() called")
        print('Setting LED channel {} to state {}'.format( channel, state ))
        self._write_to_LED(COMMANDS["set_led_channel_state"].format(channel, state))

    def led_on(self, channel):
        _unilab_logger.debug("[UNILAB] ThorlabsDC4100.led_on() called")
        print('Turning on LED channel {}'.format( channel ))
        self._write_to_LED(COMMANDS["led_on"].format(channel))
    
    def led_off(self,channel):
        _unilab_logger.debug("[UNILAB] ThorlabsDC4100.led_off() called")
        self._write_to_LED(COMMANDS["led_off"].format(channel))
    
    def set_brightness(self,channel, brightness):
        _unilab_logger.debug("[UNILAB] ThorlabsDC4100.set_brightness() called")
        self._write_to_LED(COMMANDS["set_brightness"].format(channel,brightness))
    
    def get_brightness(self,channel):
        _unilab_logger.debug("[UNILAB] ThorlabsDC4100.get_brightness() called")
        self._write_to_LED(COMMANDS["get_brightness"].format(channel))
        return self._read_from_LED()
    
    def check_if_on(self, channel):
        _unilab_logger.debug("[UNILAB] ThorlabsDC4100.check_if_on() called")
        self._write_to_LED(COMMANDS["return_on_off"].format(channel))
        return self._read_from_LED()
    
    @property
    def serial_number(self):
        _unilab_logger.debug("[UNILAB] ThorlabsDC4100.serial_number() called")
        self._write_to_LED(COMMANDS["serial_number"])
        return self._read_from_LED()

    @property
    def firmware(self):
        _unilab_logger.debug("[UNILAB] ThorlabsDC4100.firmware() called")
        self._write_to_LED(COMMANDS["firmware"])
        return self._read_from_LED()

    @property
    def manufacturer(self):
        _unilab_logger.debug("[UNILAB] ThorlabsDC4100.manufacturer() called")
        self._write_to_LED(COMMANDS["manufacturer"])
        return self._read_from_LED()

    def _write_to_LED(self, command):
        _unilab_logger.debug("[UNILAB] ThorlabsDC4100._write_to_LED() called")
        # parse string into action, channel, value
        split_string = command.split(' ')
        if split_string[0] == 'O':
            None  # do something here

    def _read_from_LED(self):
        _unilab_logger.debug("[UNILAB] ThorlabsDC4100._read_from_LED() called")
        print('Result: {}'.format( self.read_buffer) )
        self.read_buffer = []


def main():
    led = ThorlabsDC4100(port='com12',baudrate=115200,timeout=0.5)

if __name__ == "__main__":
    main()

__all__ = ["ThorlabsDC4100"]
