"""
Auto-generated by Build Agent
Original: pymepix/TPX4/tpx4controller.py

[UNILAB DEBUG] 此驱动已注入调试日志
- 每个方法被调用时会打印日志
- 日志格式: [UNILAB] ClassName.method_name() called
- 查看日志: logging.getLogger("unilab.driver").setLevel(logging.DEBUG)
"""
from typing import Dict, Any, Optional
import logging
import warnings as _warnings
_warnings.filterwarnings("ignore", message=r"pkg_resources is deprecated as an API\..*")
import sys as _sys
import types as _types

# 兼容性：部分上游框架（例如 lantz）在 import-time 强制加载 Qt（PyQt4/PySide1），
# 在 headless/现代 Python 环境中通常不存在这些绑定，会导致导入直接失败。
# 这里预注入一个最小的 `lantz.utils.qt` shim，避免触发其 Qt loader。
if "lantz.utils.qt" not in _sys.modules:
    _qt_mod = _types.ModuleType("lantz.utils.qt")

    class _DummyQtCore:
        class QObject:
            def __init__(self, *_args, **_kwargs):
                # 兼容 QObject(parent=...) 等调用
                return None

        @staticmethod
        def Signal(*_args, **_kwargs):
            return object()

        @staticmethod
        def Slot(*_args, **_kwargs):
            def _decorator(fn):
                return fn

            return _decorator

    _qt_mod.QtCore = _DummyQtCore

    class _DummySuperQObject(_DummyQtCore.QObject):
        def __new__(cls, *_args, **_kwargs):
            return super().__new__(cls)

        def __init__(self, *_args, **_kwargs):
            # 允许 Driver(...) 传参，不触发 object.__init__ 的 TypeError
            return None

    _qt_mod.MetaQObject = type
    _qt_mod.SuperQObject = _DummySuperQObject
    _sys.modules["lantz.utils.qt"] = _qt_mod

# Unilab 驱动调试日志
_unilab_logger = logging.getLogger("unilab.driver.Timepix4Controller")
_unilab_logger.setLevel(logging.DEBUG)
if not _unilab_logger.handlers:
    _handler = logging.StreamHandler()
    _handler.setFormatter(logging.Formatter("[%(asctime)s] %(message)s"))
    _unilab_logger.addHandler(_handler)

# Vendored repo packages: pymepix, pymepix
import sys as _sys
from pathlib import Path as _Path
_vendor_root = _Path(__file__).resolve().parent / "_vendor"
if _vendor_root.exists() and str(_vendor_root) not in _sys.path:
    _sys.path.insert(0, str(_vendor_root))


import socket
import threading

import numpy as np

from pymepix.core.log import Logger
from pymepix.TPX4.tpx4chipdevice import Tpx4ChipDevice

class Timepix4Controller(Logger):

    def __init__(self, dst_ip_port, pc_ip, udp_ip_port):
        print("[UNILAB] Timepix4Controller.__init__() called", flush=True)
        Logger.__init__(self, Timepix4Controller.__name__)

        self._spidrAddr = dst_ip_port
        self._pc_ip = pc_ip
        self._udp_ip_port = udp_ip_port

        self._devices = []
        self._initDevices()


    def __getitem__(self, key):
        _unilab_logger.debug("[UNILAB] Timepix4Controller.__getitem__() called")
        return self._devices[key]

    def __len__(self):
        _unilab_logger.debug("[UNILAB] Timepix4Controller.__len__() called")
        return len(self._devices)


    def _initDevices(self):
        _unilab_logger.debug("[UNILAB] Timepix4Controller._initDevices() called")

        count = self.deviceCount

        for x in range(count):
            self._devices.append(Tpx4ChipDevice(self, x))
            self._devices[x].ipAddrDest = self._udp_ip_port[0]
            self._devices[x].serverPort = self._udp_ip_port[1] + x



    @property
    def deviceCount(self):
        _unilab_logger.debug("[UNILAB] Timepix4Controller.deviceCount() called")
        # NEEDS IMPLEMENTATION

        """Returns devices count

        """
        return 1

    def setBiasSupplyEnable(self, enable):
        _unilab_logger.debug("[UNILAB] Timepix4Controller.setBiasSupplyEnable() called")
        """Enables/Disables bias supply voltage

        Parameters
        -----------
        enable : bool
            True - enables bias supply voltage
            False - disables bias supply voltage

        Raises
        ----------
        :class:`PymePixException`
            Communication error

        """

        # NEEDS IMPLEMENTATION
        pass


    def prepare(self):
        _unilab_logger.debug("[UNILAB] Timepix4Controller.prepare() called")

        # NEEDS IMPLEMENTATION
        pass

    def resetTimers(self):
        _unilab_logger.debug("[UNILAB] Timepix4Controller.resetTimers() called")
        """Resets all timers to zero

        Sets the internal 48-bit timers for all Timepix/Medipix devices to zero


        Raises
        ----------
        :class:`PymePixException`
            Communication error

        """
        # NEEDS IMPLEMENTATION
        pass

    def restartTimers(self):
        _unilab_logger.debug("[UNILAB] Timepix4Controller.restartTimers() called")
        """Restarts SPIDR and Device timers

        Synchronizes both the SPIDR clock and Timepix/Medipix clocks so both trigger
        and ToA timestamps match

        .. Important::
            This must be done if event selection is required (e.g. time of flight) otherwise
            the timestamps will be offset




        Raises
        ----------
        :class:`PymePixException`
            Communication error

        """
        # NEEDS IMPLEMENTATION
        pass

    def openShutter(self):
        _unilab_logger.debug("[UNILAB] Timepix4Controller.openShutter() called")
        """Immediately opens the shutter indefinetly

        Raises
        ----------
        :class:`PymePixException`
            Communication error

        Notes
        ---------
        This overwrites shutter configurations with one that forces
        an open shutter

        """
        # NEEDS IMPLEMENTATION
        pass

    def closeShutter(self):
        _unilab_logger.debug("[UNILAB] Timepix4Controller.closeShutter() called")
        """Immediately closes the shutter

        Raises
        ----------
        :class:`PymePixException`
            Communication error

        """
        # NEEDS IMPLEMENTATION
        pass

    def setShutterTriggerConfig(self, mode, length_us, freq_hz, count, delay_ns=0):
        _unilab_logger.debug("[UNILAB] Timepix4Controller.setShutterTriggerConfig() called")
        """Set the shutter configuration in one go


        Parameters
        ----------
        mode: int
            Shutter trigger mode

        length_us: int
            Shutter open time in microseconds

        freq_hz: int
            Auto trigger frequency in Hertz

        count: int
            Number of triggers

        delay_ns: int, optional
            Delay between each trigger (Default: 0)

        Raises
        ----------
        :class:`PymePixException`
            Communication error


        """

        # NEEDS IMPLEMENTATION
        pass

    @property
    def localTemperature(self):
        _unilab_logger.debug("[UNILAB] Timepix4Controller.localTemperature() called")
        """Local ????!?!? Temperature read from sensor

        Returns
        --------
        float:
            Temperature in Celsius


        Raises
        ----------
        :class:`PymePixException`
            Communication error

        """
        # NEEDS IMPLEMENTATION
        return 0

    @property
    def remoteTemperature(self):
        _unilab_logger.debug("[UNILAB] Timepix4Controller.remoteTemperature() called")
        """Remote ????!?!? Temperature read from sensor

        Returns
        --------
        float:
            Temperature in Celsius


        Raises
        ----------
        :class:`PymePixException`
            Communication error

        """
        # NEEDS IMPLEMENTATION
        return 0

    @property
    def fpgaTemperature(self):
        _unilab_logger.debug("[UNILAB] Timepix4Controller.fpgaTemperature() called")
        """Temperature of FPGA board read from sensor

        Returns
        --------
        float:
            Temperature in Celsius


        Raises
        ----------
        :class:`PymePixException`
            Communication error

        """
        # NEEDS IMPLEMENTATION
        return 0

    @property
    def humidity(self):
        _unilab_logger.debug("[UNILAB] Timepix4Controller.humidity() called")
        """Humidity read from sensor

        Returns
        --------
        int:
            Humidity as percentage


        Raises
        ----------
        :class:`PymePixException`
            Communication error

        """
        # NEEDS IMPLEMENTATION
        return 0

    @property
    def pressure(self):
        _unilab_logger.debug("[UNILAB] Timepix4Controller.pressure() called")
        """Pressure read from sensor

        Returns
        --------
        int:
            Pressure in bar


        Raises
        ----------
        :class:`PymePixException`
            Communication error

        """
        # NEEDS IMPLEMENTATION
        return 0

    @property
    def chipboardFanSpeed(self):
        _unilab_logger.debug("[UNILAB] Timepix4Controller.chipboardFanSpeed() called")
        # NEEDS IMPLEMENTATION
        return 0

    @property
    def boardFanSpeed(self):
        _unilab_logger.debug("[UNILAB] Timepix4Controller.boardFanSpeed() called")
        # NEEDS IMPLEMENTATION
        return 0

__all__ = ["Timepix4Controller"]
