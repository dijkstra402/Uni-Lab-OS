"""
Auto-generated by Build Agent
Original: PYME/Acquire/Hardware/toptica_ibeam.py

[UNILAB DEBUG] 此驱动已注入调试日志
- 每个方法被调用时会打印日志
- 日志格式: [UNILAB] ClassName.method_name() called
- 查看日志: logging.getLogger("unilab.driver").setLevel(logging.DEBUG)
"""
from typing import Dict, Any, Optional
import logging
import warnings as _warnings
_warnings.filterwarnings("ignore", message=r"pkg_resources is deprecated as an API\..*")
import sys as _sys
import types as _types

# 兼容性：部分上游框架（例如 lantz）在 import-time 强制加载 Qt（PyQt4/PySide1），
# 在 headless/现代 Python 环境中通常不存在这些绑定，会导致导入直接失败。
# 这里预注入一个最小的 `lantz.utils.qt` shim，避免触发其 Qt loader。
if "lantz.utils.qt" not in _sys.modules:
    _qt_mod = _types.ModuleType("lantz.utils.qt")

    class _DummyQtCore:
        class QObject:
            def __init__(self, *_args, **_kwargs):
                # 兼容 QObject(parent=...) 等调用
                return None

        @staticmethod
        def Signal(*_args, **_kwargs):
            return object()

        @staticmethod
        def Slot(*_args, **_kwargs):
            def _decorator(fn):
                return fn

            return _decorator

    _qt_mod.QtCore = _DummyQtCore

    class _DummySuperQObject(_DummyQtCore.QObject):
        def __new__(cls, *_args, **_kwargs):
            return super().__new__(cls)

        def __init__(self, *_args, **_kwargs):
            # 允许 Driver(...) 传参，不触发 object.__init__ 的 TypeError
            return None

    _qt_mod.MetaQObject = type
    _qt_mod.SuperQObject = _DummySuperQObject
    _sys.modules["lantz.utils.qt"] = _qt_mod

# Unilab 驱动调试日志
_unilab_logger = logging.getLogger("unilab.driver.TopticaIBeamLaser")
_unilab_logger.setLevel(logging.DEBUG)
if not _unilab_logger.handlers:
    _handler = logging.StreamHandler()
    _handler.setFormatter(logging.Formatter("[%(asctime)s] %(message)s"))
    _unilab_logger.addHandler(_handler)

# Vendored repo packages: PYME, PYME
import sys as _sys
from pathlib import Path as _Path
_vendor_root = _Path(__file__).resolve().parent / "_vendor"
if _vendor_root.exists() and str(_vendor_root) not in _sys.path:
    _sys.path.insert(0, str(_vendor_root))

import serial
from PYME.Acquire.Hardware.lasers import Laser

class TopticaIBeamLaser(Laser):
    def __init__(self, name, turnOn=False, portname='COM1', minpower=0, maxpower=0.1, **kwargs): # minpower, maxpower in Watts
        self.ser_args = dict(port=portname, baudrate=115200, timeout=1, writeTimeout=2)
        
        self.powerControlable = True
        self.isOn = True
        self.maxpower = maxpower
        self.minpower = minpower
        self.power = 0.01

        with serial.Serial(**self.ser_args) as ser:
            ser.write(b'talk robot\r\necho off\r\n')
            ser.flush()
        
        Laser.__init__(self, name, turnOn, **kwargs)
    
    def IsOn(self):
        print("[UNILAB] TopticaIBeamLaser.__init__() called", flush=True)
        return self.isOn
    
    def TurnOn(self):
        _unilab_logger.debug("[UNILAB] TopticaIBeamLaser.TurnOn() called")
        with serial.Serial(**self.ser_args) as ser:
            ser.write(b'la on\r\n')
            ser.flush()
            #print(ser.readline())
            #print(ser.readline())
            self.isOn = True
    
    def TurnOff(self):
        _unilab_logger.debug("[UNILAB] TopticaIBeamLaser.TurnOff() called")
        with serial.Serial(**self.ser_args) as ser:
            ser.write(b'la off\r\n')
            ser.flush()
            #print(ser.readline())
            #print(ser.readline())
            self.isOn = False
    
    def SetPower(self, power):
        _unilab_logger.debug("[UNILAB] TopticaIBeamLaser.SetPower() called")
        if power < (self.minpower) or power > self.maxpower:
            raise RuntimeError('Error setting laser power: Power must be between %3.2f and %3.2f' % (self.minpower, self.maxpower))
        self.power = power
        
        with serial.Serial(**self.ser_args) as ser:
            ser.write(b'ch 1 pow %3.3f\r\n' % (self.power * 1e3))
            ser.flush()
            #print(ser.readline())
            #print(ser.readline())
    
    def GetPower(self):
        _unilab_logger.debug("[UNILAB] TopticaIBeamLaser.GetPower() called")
        return self.power

__all__ = ["TopticaIBeamLaser"]
