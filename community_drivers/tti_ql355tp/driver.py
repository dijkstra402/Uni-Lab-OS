"""
Auto-generated by Build Agent
Original: basil/HL/tti_ql355tp.py

[UNILAB DEBUG] 此驱动已注入调试日志
- 每个方法被调用时会打印日志
- 日志格式: [UNILAB] ClassName.method_name() called
- 查看日志: logging.getLogger("unilab.driver").setLevel(logging.DEBUG)
"""
from typing import Dict, Any, Optional
import logging
import warnings as _warnings
_warnings.filterwarnings("ignore", message=r"pkg_resources is deprecated as an API\..*")
import sys as _sys
import types as _types

# 兼容性：部分上游框架（例如 lantz）在 import-time 强制加载 Qt（PyQt4/PySide1），
# 在 headless/现代 Python 环境中通常不存在这些绑定，会导致导入直接失败。
# 这里预注入一个最小的 `lantz.utils.qt` shim，避免触发其 Qt loader。
if "lantz.utils.qt" not in _sys.modules:
    _qt_mod = _types.ModuleType("lantz.utils.qt")

    class _DummyQtCore:
        class QObject:
            def __init__(self, *_args, **_kwargs):
                # 兼容 QObject(parent=...) 等调用
                return None

        @staticmethod
        def Signal(*_args, **_kwargs):
            return object()

        @staticmethod
        def Slot(*_args, **_kwargs):
            def _decorator(fn):
                return fn

            return _decorator

    _qt_mod.QtCore = _DummyQtCore

    class _DummySuperQObject(_DummyQtCore.QObject):
        def __new__(cls, *_args, **_kwargs):
            return super().__new__(cls)

        def __init__(self, *_args, **_kwargs):
            # 允许 Driver(...) 传参，不触发 object.__init__ 的 TypeError
            return None

    _qt_mod.MetaQObject = type
    _qt_mod.SuperQObject = _DummySuperQObject
    _sys.modules["lantz.utils.qt"] = _qt_mod

# Unilab 驱动调试日志
_unilab_logger = logging.getLogger("unilab.driver.ttiQl355tp")
_unilab_logger.setLevel(logging.DEBUG)
if not _unilab_logger.handlers:
    _handler = logging.StreamHandler()
    _handler.setFormatter(logging.Formatter("[%(asctime)s] %(message)s"))
    _unilab_logger.addHandler(_handler)

# Vendored repo packages: basil, basil
import sys as _sys
from pathlib import Path as _Path
_vendor_root = _Path(__file__).resolve().parent / "_vendor"
if _vendor_root.exists() and str(_vendor_root) not in _sys.path:
    _sys.path.insert(0, str(_vendor_root))

#
# ------------------------------------------------------------
# Copyright (c) All rights reserved
# SiLab, Institute of Physics, University of Bonn
# ------------------------------------------------------------
#

import logging
import time

from basil.HL.RegisterHardwareLayer import HardwareLayer


logger = logging.getLogger(__name__)


class ttiQl355tp(HardwareLayer):
    ''' HL for the TTi QL355TP.
    '''

    def __init__(self, intf, conf):
        print("[UNILAB] ttiQl355tp.__init__() called", flush=True)
        super(ttiQl355tp, self).__init__(intf, conf)

    def init(self):
        _unilab_logger.debug("[UNILAB] ttiQl355tp.init() called")
        super(ttiQl355tp, self).init()

    def reinit(self):
        _unilab_logger.debug("[UNILAB] ttiQl355tp.reinit() called")
        self._intf.init()

    def write(self, command):
        _unilab_logger.debug("[UNILAB] ttiQl355tp.write() called")
        self._intf.write(command)

    def ask(self, command):
        _unilab_logger.debug("[UNILAB] ttiQl355tp.ask() called")
        self.write(command)
        time.sleep(0.1)
        return self.read()

    def read(self):
        _unilab_logger.debug("[UNILAB] ttiQl355tp.read() called")
        ret = self._intf.read()
        if ret[-2:] != "\r\n":
            logger.warning("ttiTp355tp.read() termination error")
        return ret[:-2]

    def set_enable(self, on, channel=1):
        _unilab_logger.debug("[UNILAB] ttiQl355tp.set_enable() called")
        """ channel: 1=OP1, 2=OP2, 3=AUX, ALL=all channels"""
        if isinstance(channel, str):
            cmd = "OPALL %d" % int(on)
        elif isinstance(channel, int):
            cmd = "OP%d %d" % (channel, int(on))
        self.write(cmd)

    def get_name(self):
        _unilab_logger.debug("[UNILAB] ttiQl355tp.get_name() called")
        return self.ask("*IDN?")

    def get_current(self, channel):
        _unilab_logger.debug("[UNILAB] ttiQl355tp.get_current() called")
        """ channel: 1=OP1, 2=OP2, AUX is not supported"""
        ret = self.ask("I%dO?" % channel)
        if ret[-1] != "A":
            logger.warning("ttiQl355tp.get_current() format error: %s" % ret)
            return None
        return float(ret[:-1])

    def get_voltage(self, channel):
        _unilab_logger.debug("[UNILAB] ttiQl355tp.get_voltage() called")
        """ channel: 1=OP1, 2=OP2, AUX is not supported"""
        ret = self.ask("V%dO?" % channel)
        if ret[-1] != "V":
            logger.warning("ttiQl355tp.get_voltage() format error: %s" % ret)
            return None
        return float(ret[:-1])

    def get_set_voltage(self, channel):
        _unilab_logger.debug("[UNILAB] ttiQl355tp.get_set_voltage() called")
        """ channel: 1=OP1, 2=OP2, AUX is not supported"""
        ret = self.ask("V%d?" % channel)
        if ret[:3] != "V%d " % channel:
            logger.warning("ttiQl355tp.get_voltage() format error: %s" % ret)
            return None
        return float(ret[3:])

    def get_current_limit(self, channel):
        _unilab_logger.debug("[UNILAB] ttiQl355tp.get_current_limit() called")
        """ channel: 1=OP1, 2=OP2, AUX is not supported"""
        ret = self.ask("I%d?" % channel)
        if ret[:3] != "I%d " % channel:
            logger.warning("ttiQl355tp.get_current_limit() format error: %s" % ret)
            return None
        return float(ret[3:])

    def set_voltage(self, value, channel=1):
        _unilab_logger.debug("[UNILAB] ttiQl355tp.set_voltage() called")
        """ channel: 1=OP1, 2=OP2, AUX is not supported"""
        cmd = "V%d %f" % (channel, value)
        self.write(cmd)

    def set_current_limit(self, value, channel=1):
        _unilab_logger.debug("[UNILAB] ttiQl355tp.set_current_limit() called")
        """ channel: 1=OP1, 2=OP2, AUX is not supported"""
        cmd = "I%d %f" % (channel, value)
        self.write(cmd)

    def reset_trip(self):
        _unilab_logger.debug("[UNILAB] ttiQl355tp.reset_trip() called")
        cmd = "TRIPRST"
        self.write(cmd)

__all__ = ["ttiQl355tp"]
