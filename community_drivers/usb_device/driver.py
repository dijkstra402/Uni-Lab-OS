"""
Auto-generated by Build Agent
Original: drivers/usb_device.py

[UNILAB DEBUG] 此驱动已注入调试日志
- 每个方法被调用时会打印日志
- 日志格式: [UNILAB] ClassName.method_name() called
- 查看日志: logging.getLogger("unilab.driver").setLevel(logging.DEBUG)
"""
from typing import Dict, Any, Optional
import logging
import warnings as _warnings
_warnings.filterwarnings("ignore", message=r"pkg_resources is deprecated as an API\..*")
import sys as _sys
import types as _types

# 兼容性：部分上游框架（例如 lantz）在 import-time 强制加载 Qt（PyQt4/PySide1），
# 在 headless/现代 Python 环境中通常不存在这些绑定，会导致导入直接失败。
# 这里预注入一个最小的 `lantz.utils.qt` shim，避免触发其 Qt loader。
if "lantz.utils.qt" not in _sys.modules:
    _qt_mod = _types.ModuleType("lantz.utils.qt")

    class _DummyQtCore:
        class QObject:
            def __init__(self, *_args, **_kwargs):
                # 兼容 QObject(parent=...) 等调用
                return None

        @staticmethod
        def Signal(*_args, **_kwargs):
            return object()

        @staticmethod
        def Slot(*_args, **_kwargs):
            def _decorator(fn):
                return fn

            return _decorator

    _qt_mod.QtCore = _DummyQtCore

    class _DummySuperQObject(_DummyQtCore.QObject):
        def __new__(cls, *_args, **_kwargs):
            return super().__new__(cls)

        def __init__(self, *_args, **_kwargs):
            # 允许 Driver(...) 传参，不触发 object.__init__ 的 TypeError
            return None

    _qt_mod.MetaQObject = type
    _qt_mod.SuperQObject = _DummySuperQObject
    _sys.modules["lantz.utils.qt"] = _qt_mod

# Unilab 驱动调试日志
_unilab_logger = logging.getLogger("unilab.driver.UsbDevice")
_unilab_logger.setLevel(logging.DEBUG)
if not _unilab_logger.handlers:
    _handler = logging.StreamHandler()
    _handler.setFormatter(logging.Formatter("[%(asctime)s] %(message)s"))
    _unilab_logger.addHandler(_handler)

# Vendored repo packages: drivers
import sys as _sys
from pathlib import Path as _Path
_vendor_root = _Path(__file__).resolve().parent / "_vendor"
if _vendor_root.exists() and str(_vendor_root) not in _sys.path:
    _sys.path.insert(0, str(_vendor_root))

import usb.core
import usb.util
import time

class UsbDevice:
    def __init__(self, id_vendor, id_product):
        print("[UNILAB] UsbDevice.__init__() called", flush=True)
        dev = usb.core.find(idVendor=id_vendor, idProduct=id_product)
        self._dev = dev
        if dev is None:
            raise ValueError('Device not found')

        # set the active configuration. With no arguments, the first
        # configuration will be the active one
        dev.set_configuration()

        # get an endpoint instance
        cfg = dev.get_active_configuration()
        intf = cfg[(0,0)]

        self._ep_out = usb.util.find_descriptor(
            intf,
            custom_match = \
            lambda e: \
                usb.util.endpoint_direction(e.bEndpointAddress) == \
                usb.util.ENDPOINT_OUT)

        self._ep_in = usb.util.find_descriptor(
            intf,
            custom_match = \
            lambda e: \
                usb.util.endpoint_direction(e.bEndpointAddress) == \
                usb.util.ENDPOINT_IN)

        assert self._ep_out is not None
        assert self._ep_in is not None

    def write(self, data):
        _unilab_logger.debug("[UNILAB] UsbDevice.write() called")
        return self._dev.write(self._ep_out.bEndpointAddress, data)

    def read_raw(self):
        _unilab_logger.debug("[UNILAB] UsbDevice.read_raw() called")
        data_raw = self._dev.read(self._ep_in.bEndpointAddress, self._ep_in.wMaxPacketSize)
        return data_raw

    def read(self):
        _unilab_logger.debug("[UNILAB] UsbDevice.read() called")
        data_raw = self.read_raw()
        data = ''.join([chr(d) for d in data_raw])
        return data

    def write_read(self, data):
        _unilab_logger.debug("[UNILAB] UsbDevice.write_read() called")
        self.write(data)
        return self.read()

    def write_read_raw(self, data):
        _unilab_logger.debug("[UNILAB] UsbDevice.write_read_raw() called")
        self.write(data)
        return self.read_raw()

__all__ = ["UsbDevice"]
