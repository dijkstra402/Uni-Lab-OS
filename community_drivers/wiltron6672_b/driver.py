"""
Auto-generated by Build Agent
Original: src/labtoolkit/SignalGenerator/Wiltron6672B.py

[UNILAB DEBUG] 此驱动已注入调试日志
- 每个方法被调用时会打印日志
- 日志格式: [UNILAB] ClassName.method_name() called
- 查看日志: logging.getLogger("unilab.driver").setLevel(logging.DEBUG)
"""
from typing import Dict, Any, Optional
import logging
import warnings as _warnings
_warnings.filterwarnings("ignore", message=r"pkg_resources is deprecated as an API\..*")
import sys as _sys
import types as _types

# 兼容性：部分上游框架（例如 lantz）在 import-time 强制加载 Qt（PyQt4/PySide1），
# 在 headless/现代 Python 环境中通常不存在这些绑定，会导致导入直接失败。
# 这里预注入一个最小的 `lantz.utils.qt` shim，避免触发其 Qt loader。
if "lantz.utils.qt" not in _sys.modules:
    _qt_mod = _types.ModuleType("lantz.utils.qt")

    class _DummyQtCore:
        class QObject:
            def __init__(self, *_args, **_kwargs):
                # 兼容 QObject(parent=...) 等调用
                return None

        @staticmethod
        def Signal(*_args, **_kwargs):
            return object()

        @staticmethod
        def Slot(*_args, **_kwargs):
            def _decorator(fn):
                return fn

            return _decorator

    _qt_mod.QtCore = _DummyQtCore

    class _DummySuperQObject(_DummyQtCore.QObject):
        def __new__(cls, *_args, **_kwargs):
            return super().__new__(cls)

        def __init__(self, *_args, **_kwargs):
            # 允许 Driver(...) 传参，不触发 object.__init__ 的 TypeError
            return None

    _qt_mod.MetaQObject = type
    _qt_mod.SuperQObject = _DummySuperQObject
    _sys.modules["lantz.utils.qt"] = _qt_mod

# Unilab 驱动调试日志
_unilab_logger = logging.getLogger("unilab.driver.Wiltron6672B")
_unilab_logger.setLevel(logging.DEBUG)
if not _unilab_logger.handlers:
    _handler = logging.StreamHandler()
    _handler.setFormatter(logging.Formatter("[%(asctime)s] %(message)s"))
    _unilab_logger.addHandler(_handler)

# Vendored repo packages: labtoolkit, labtoolkit.SignalGenerator.helper, labtoolkit
import sys as _sys
from pathlib import Path as _Path
_vendor_root = _Path(__file__).resolve().parent / "_vendor"
if _vendor_root.exists() and str(_vendor_root) not in _sys.path:
    _sys.path.insert(0, str(_vendor_root))

from labtoolkit.Instrument import Instrument
from labtoolkit.SignalGenerator.helper import SignalGenerator, amplitudelimiter


class Wiltron6672B(Instrument, SignalGenerator):
    """Wiltron 6672B 40e9, 60e9.

    .. figure::  images/SignalGenerator/Wiltron6672B.jpg
    """

    def __init__(self, *args, **kwargs):
        print("[UNILAB] Wiltron6672B.__init__() called", flush=True)
        super().__init__(*args, **kwargs)
    def __post__(self):
        _unilab_logger.debug("[UNILAB] Wiltron6672B.__post__() called")
        """."""

        self.amps = [-20, 17]
        self.freqs = [40e9, 60e9]
        # self.siggen.write("*CLS")  # clear error status
        # self.frequency = min(self.freqs)

    @property
    def frequency(self):
        _unilab_logger.debug("[UNILAB] Wiltron6672B.frequency() called")
        """."""
        return(self.query("OF0"))

    @frequency.setter
    def frequency(self, frequency):
        _unilab_logger.debug("[UNILAB] Wiltron6672B.frequency() called")
        self.write(f"F0{frequency:.2f}GH")

    @property
    def amplitude(self):
        _unilab_logger.debug("[UNILAB] Wiltron6672B.amplitude() called")
        """."""
        return(self.query("OL0"))

    @amplitude.setter
    @amplitudelimiter
    def amplitude(self, amplitude):
        _unilab_logger.debug("[UNILAB] Wiltron6672B.amplitude() called")
        self.write(f"L0{amplitude:.2f}DM")

    '''@property
    def output(self):
        _unilab_logger.debug("[UNILAB] Wiltron6672B.output() called")
        if self.query("OUTPut:STATe?") == "1":
            return(True)
        else:
            return(False)

    @output.setter
    def output(self, boolean=False):
        _unilab_logger.debug("[UNILAB] Wiltron6672B.output() called")
        self.write("OUTPut:STATe {:d}".format(boolean))
    '''

__all__ = ["Wiltron6672B"]
