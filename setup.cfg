[install]
install_scripts=$base/lib/unilabos

[tool:pytest]
testpaths = tests
asyncio_mode = auto
markers =
    integration: tests that require external services or hardware
    slow: tests that are intentionally long-running
