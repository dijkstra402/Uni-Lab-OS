"""
Auto-generated by Build Agent
Original: src/Switch-Bentham_610/libs/bendev/file_device.py

[UNILAB DEBUG] 此驱动已注入调试日志
- 每个方法被调用时会打印日志
- 日志格式: [UNILAB] ClassName.method_name() called
- 查看日志: logging.getLogger("unilab.driver").setLevel(logging.DEBUG)
"""
from typing import Dict, Any, Optional
import logging
import warnings as _warnings
_warnings.filterwarnings("ignore", message=r"pkg_resources is deprecated as an API\..*")
import sys as _sys
import types as _types

# 兼容性：部分上游框架（例如 lantz）在 import-time 强制加载 Qt（PyQt4/PySide1），
# 在 headless/现代 Python 环境中通常不存在这些绑定，会导致导入直接失败。
# 这里预注入一个最小的 `lantz.utils.qt` shim，避免触发其 Qt loader。
if "lantz.utils.qt" not in _sys.modules:
    _qt_mod = _types.ModuleType("lantz.utils.qt")

    class _DummyQtCore:
        class QObject:
            def __init__(self, *_args, **_kwargs):
                # 兼容 QObject(parent=...) 等调用
                return None

        @staticmethod
        def Signal(*_args, **_kwargs):
            return object()

        @staticmethod
        def Slot(*_args, **_kwargs):
            def _decorator(fn):
                return fn

            return _decorator

    _qt_mod.QtCore = _DummyQtCore

    class _DummySuperQObject(_DummyQtCore.QObject):
        def __new__(cls, *_args, **_kwargs):
            return super().__new__(cls)

        def __init__(self, *_args, **_kwargs):
            # 允许 Driver(...) 传参，不触发 object.__init__ 的 TypeError
            return None

    _qt_mod.MetaQObject = type
    _qt_mod.SuperQObject = _DummySuperQObject
    _sys.modules["lantz.utils.qt"] = _qt_mod

# Unilab 驱动调试日志
_unilab_logger = logging.getLogger("unilab.driver.FileDevice")
_unilab_logger.setLevel(logging.DEBUG)
if not _unilab_logger.handlers:
    _handler = logging.StreamHandler()
    _handler.setFormatter(logging.Formatter("[%(asctime)s] %(message)s"))
    _unilab_logger.addHandler(_handler)

import os, select

class FileDevice:
    def __init__(self, path):
        print("[UNILAB] FileDevice.__init__() called", flush=True)
        self.file_handle = os.open(path, os.O_RDWR)

    def write(self, data, timeout=0.5):
        _unilab_logger.debug("[UNILAB] FileDevice.write() called")
        lR,lW,lX = select.select([],[self.file_handle],[],timeout)
        if lW:
            os.write(self.file_handle, data)
        else:
            raise TimeoutError(f"Device timeout, unavailable for writing for {timeout} seconds")
    
    def read(self, max_characters=64, timeout=None):
        _unilab_logger.debug("[UNILAB] FileDevice.read() called")
        if timeout==0:
            timeout = None
        lR,lW,lX = select.select([self.file_handle],[],[], timeout)
        if not lR:
            return ""
        return os.read(self.file_handle, max_characters)

    def close(self):
        _unilab_logger.debug("[UNILAB] FileDevice.close() called")
        try:
            os.close(self.file_handle)
        except OSError:
            pass

    def __del__(self):
        _unilab_logger.debug("[UNILAB] FileDevice.__del__() called")
        self.close()

if __name__ == "__main__":
    f = FileDevice("/dev/hidraw2")
    f.write(b"*IDN?")
    print (f.read())
    # print (f.read(1))

__all__ = ["FileDevice"]
