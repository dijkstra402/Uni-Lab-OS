"""
Auto-generated by Build Agent
Original: basil/HL/julaboF32HD.py

[UNILAB DEBUG] 此驱动已注入调试日志
- 每个方法被调用时会打印日志
- 日志格式: [UNILAB] ClassName.method_name() called
- 查看日志: logging.getLogger("unilab.driver").setLevel(logging.DEBUG)
"""
from typing import Dict, Any, Optional
import logging
import warnings as _warnings
_warnings.filterwarnings("ignore", message=r"pkg_resources is deprecated as an API\..*")
import sys as _sys
import types as _types

# 兼容性：部分上游框架（例如 lantz）在 import-time 强制加载 Qt（PyQt4/PySide1），
# 在 headless/现代 Python 环境中通常不存在这些绑定，会导致导入直接失败。
# 这里预注入一个最小的 `lantz.utils.qt` shim，避免触发其 Qt loader。
if "lantz.utils.qt" not in _sys.modules:
    _qt_mod = _types.ModuleType("lantz.utils.qt")

    class _DummyQtCore:
        class QObject:
            def __init__(self, *_args, **_kwargs):
                # 兼容 QObject(parent=...) 等调用
                return None

        @staticmethod
        def Signal(*_args, **_kwargs):
            return object()

        @staticmethod
        def Slot(*_args, **_kwargs):
            def _decorator(fn):
                return fn

            return _decorator

    _qt_mod.QtCore = _DummyQtCore

    class _DummySuperQObject(_DummyQtCore.QObject):
        def __new__(cls, *_args, **_kwargs):
            return super().__new__(cls)

        def __init__(self, *_args, **_kwargs):
            # 允许 Driver(...) 传参，不触发 object.__init__ 的 TypeError
            return None

    _qt_mod.MetaQObject = type
    _qt_mod.SuperQObject = _DummySuperQObject
    _sys.modules["lantz.utils.qt"] = _qt_mod

# Unilab 驱动调试日志
_unilab_logger = logging.getLogger("unilab.driver.julaboF32HD")
_unilab_logger.setLevel(logging.DEBUG)
if not _unilab_logger.handlers:
    _handler = logging.StreamHandler()
    _handler.setFormatter(logging.Formatter("[%(asctime)s] %(message)s"))
    _unilab_logger.addHandler(_handler)

# Vendored repo packages: basil, basil
import sys as _sys
from pathlib import Path as _Path
_vendor_root = _Path(__file__).resolve().parent / "_vendor"
if _vendor_root.exists() and str(_vendor_root) not in _sys.path:
    _sys.path.insert(0, str(_vendor_root))

#
# ------------------------------------------------------------
# Copyright (c) All rights reserved
# SiLab, Institute of Physics, University of Bonn
# ------------------------------------------------------------
#

import logging
import time

from basil.HL.RegisterHardwareLayer import HardwareLayer


logger = logging.getLogger(__name__)


class julaboF32HD(HardwareLayer):
    ''' Driver for the Julabo F32-HD chiller.
    A simple protocol via RS 232 serial port is used with 4800 baud rate.
    '''

    def __init__(self, intf, conf):
        print("[UNILAB] julaboF32HD.__init__() called", flush=True)
        super(julaboF32HD, self).__init__(intf, conf)
        self.pre_time = time.time()

    def init(self):
        _unilab_logger.debug("[UNILAB] julaboF32HD.init() called")
        super(julaboF32HD, self).init()

    def read(self):
        _unilab_logger.debug("[UNILAB] julaboF32HD.read() called")
        ret = self._intf.read()
        if len(ret) < 2 or ret[-2:] != "\r\n":
            logger.warning("read() termination error")
        return ret[:-2]

    def write(self, cmd):
        _unilab_logger.debug("[UNILAB] julaboF32HD.write() called")
        if time.time() - self.pre_time < 1.0:
            time.sleep(1.0)
        self._intf.write(str(cmd))
        self.pre_time = time.time()

    def get_identifier(self):
        _unilab_logger.debug("[UNILAB] julaboF32HD.get_identifier() called")
        ''' Read identifier
        '''
        self.write("version")
        ret = self.read()
        return ret

    def start_thermostat(self, start=True):
        _unilab_logger.debug("[UNILAB] julaboF32HD.start_thermostat() called")
        ''' Start chiller
        '''
        if start is True:
            self.write("out_mode_05 1")
        else:
            self.write("out_mode_05 0")

    def stop_thermostat(self):
        _unilab_logger.debug("[UNILAB] julaboF32HD.stop_thermostat() called")
        ''' Stop chiller
        '''
        self.start_thermostat(False)

    def get_status(self):
        _unilab_logger.debug("[UNILAB] julaboF32HD.get_status() called")
        ''' Get status
        '''
        self.write("status")
        ret = self.read()
        logger.debug("status:{:s}".format(ret))
        try:
            tmp = ret.split(" ", 1)
            status = int(tmp[0])
            status_str = tmp[1:]
        except (ValueError, AttributeError):
            logger.warning("get_status() wrong format: {}".format(repr(ret)))
            status = -99
            status_str = ret
        return status, status_str

__all__ = ["julaboF32HD"]
