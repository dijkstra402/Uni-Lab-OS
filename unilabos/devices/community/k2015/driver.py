"""
Auto-generated by Build Agent
Original: K2015.py

[UNILAB DEBUG] 此驱动已注入调试日志
- 每个方法被调用时会打印日志
- 日志格式: [UNILAB] ClassName.method_name() called
- 查看日志: logging.getLogger("unilab.driver").setLevel(logging.DEBUG)
"""
from typing import Dict, Any, Optional
import logging
import warnings as _warnings
_warnings.filterwarnings("ignore", message=r"pkg_resources is deprecated as an API\..*")
import sys as _sys
import types as _types

# 兼容性：部分上游框架（例如 lantz）在 import-time 强制加载 Qt（PyQt4/PySide1），
# 在 headless/现代 Python 环境中通常不存在这些绑定，会导致导入直接失败。
# 这里预注入一个最小的 `lantz.utils.qt` shim，避免触发其 Qt loader。
if "lantz.utils.qt" not in _sys.modules:
    _qt_mod = _types.ModuleType("lantz.utils.qt")

    class _DummyQtCore:
        class QObject:
            def __init__(self, *_args, **_kwargs):
                # 兼容 QObject(parent=...) 等调用
                return None

        @staticmethod
        def Signal(*_args, **_kwargs):
            return object()

        @staticmethod
        def Slot(*_args, **_kwargs):
            def _decorator(fn):
                return fn

            return _decorator

    _qt_mod.QtCore = _DummyQtCore

    class _DummySuperQObject(_DummyQtCore.QObject):
        def __new__(cls, *_args, **_kwargs):
            return super().__new__(cls)

        def __init__(self, *_args, **_kwargs):
            # 允许 Driver(...) 传参，不触发 object.__init__ 的 TypeError
            return None

    _qt_mod.MetaQObject = type
    _qt_mod.SuperQObject = _DummySuperQObject
    _sys.modules["lantz.utils.qt"] = _qt_mod

# Unilab 驱动调试日志
_unilab_logger = logging.getLogger("unilab.driver.K2015")
_unilab_logger.setLevel(logging.DEBUG)
if not _unilab_logger.handlers:
    _handler = logging.StreamHandler()
    _handler.setFormatter(logging.Formatter("[%(asctime)s] %(message)s"))
    _unilab_logger.addHandler(_handler)

import serial
from enum import Enum 
import time

class resistance_method(Enum):
    two_wire=0
    four_wire=1

class K2015:

    def __init__(self,addr,mode="K2015"):
        print("[UNILAB] K2015.__init__() called", flush=True)
        self.ser=serial.Serial(addr,9600,timeout=5)
        self.ser.setDTR(False)
        self.ser.setRTS(False)
        print(self.serialASK("*IDN?"))

    def beep(self):
        _unilab_logger.debug("[UNILAB] K2015.beep() called")
        self.ser.write("SYST:BEEP".encode("utf-8"))

    def resistance(self,mode:resistance_method=resistance_method.two_wire):
        _unilab_logger.debug("[UNILAB] K2015.resistance() called")
        if(mode==resistance_method.two_wire):
            return float(self.serialASK(":MEAS:RES?"))
        if(mode==resistance_method.four_wire):
            return float(self.serialASK(":MEAS:FRES?"))

    def setresistance(self,mode:resistance_method=resistance_method.two_wire):
        _unilab_logger.debug("[UNILAB] K2015.setresistance() called")
        if(mode==resistance_method.two_wire):
            self.serialWrite(":CONF:RES")
        if(mode==resistance_method.four_wire):
            self.serialWrite(":CONF:FRES")
    
    def read(self):
        _unilab_logger.debug("[UNILAB] K2015.read() called")
        return float(self.serialASK(":READ?"))

    def serialWrite(self,cmd):
        _unilab_logger.debug("[UNILAB] K2015.serialWrite() called")
        self.ser.write((cmd+"\r").encode("utf-8"))

    def serialASK(self,cmd):
        _unilab_logger.debug("[UNILAB] K2015.serialASK() called")
        self.ser.write((cmd+"\r").encode("utf-8"))
        return(self.ser.readline().decode().replace("\r\n",""))

# if __name__ == "__main__":
    # K2015X=K2015("COM9")
    # print(K2015X.setresistance(resistance_method.two_wire))
    # time.sleep(5)
    # print(K2015X.read())
    

__all__ = ["K2015"]
