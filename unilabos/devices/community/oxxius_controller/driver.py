"""
Auto-generated by Build Agent
Original: devices/LaserDevice.py

[UNILAB DEBUG] 此驱动已注入调试日志
- 每个方法被调用时会打印日志
- 日志格式: [UNILAB] ClassName.method_name() called
- 查看日志: logging.getLogger("unilab.driver").setLevel(logging.DEBUG)
"""
from typing import Dict, Any, Optional
import logging
import warnings as _warnings
_warnings.filterwarnings("ignore", message=r"pkg_resources is deprecated as an API\..*")
import sys as _sys
import types as _types

# 兼容性：部分上游框架（例如 lantz）在 import-time 强制加载 Qt（PyQt4/PySide1），
# 在 headless/现代 Python 环境中通常不存在这些绑定，会导致导入直接失败。
# 这里预注入一个最小的 `lantz.utils.qt` shim，避免触发其 Qt loader。
if "lantz.utils.qt" not in _sys.modules:
    _qt_mod = _types.ModuleType("lantz.utils.qt")

    class _DummyQtCore:
        class QObject:
            def __init__(self, *_args, **_kwargs):
                # 兼容 QObject(parent=...) 等调用
                return None

        @staticmethod
        def Signal(*_args, **_kwargs):
            return object()

        @staticmethod
        def Slot(*_args, **_kwargs):
            def _decorator(fn):
                return fn

            return _decorator

    _qt_mod.QtCore = _DummyQtCore

    class _DummySuperQObject(_DummyQtCore.QObject):
        def __new__(cls, *_args, **_kwargs):
            return super().__new__(cls)

        def __init__(self, *_args, **_kwargs):
            # 允许 Driver(...) 传参，不触发 object.__init__ 的 TypeError
            return None

    _qt_mod.MetaQObject = type
    _qt_mod.SuperQObject = _DummySuperQObject
    _sys.modules["lantz.utils.qt"] = _qt_mod

# Unilab 驱动调试日志
_unilab_logger = logging.getLogger("unilab.driver.OxxiusController")
_unilab_logger.setLevel(logging.DEBUG)
if not _unilab_logger.handlers:
    _handler = logging.StreamHandler()
    _handler.setFormatter(logging.Formatter("[%(asctime)s] %(message)s"))
    _unilab_logger.addHandler(_handler)

# Vendored repo packages: devices
import sys as _sys
from pathlib import Path as _Path
_vendor_root = _Path(__file__).resolve().parent / "_vendor"
if _vendor_root.exists() and str(_vendor_root) not in _sys.path:
    _sys.path.insert(0, str(_vendor_root))

"""
Intro:  Device driver for laser control with serial commands.
        Laser models: Oxxius LBX Diode laser, LCX DPSS laser
Author: Hai Gong
Email:  h.gong@imperial.ac.uk
Time:   Oct 2020
"""

import serial
import sys
from time import sleep
cmd_lbx = {'analog_control_mode': 'ACC',
           'analog_modulation_state': 'AM',
           'base_plate_temp': 'BT',
           'laser_diode_current': 'C',
           'cdrh_state': 'CDRH',
           'modulation_state': 'CW',
           'diode_temp_set_point': 'DST',
           'measured_diode_temp': 'DT',
           'fault_number': 'F',
           'cum_time_of_operation': 'HH',
           'serial_num_and_wavelength': 'HID',
           'interlock_state': 'INT',
           'input_voltage': 'IV',
           'laser_emission_activation': 'L',
           'max_laser_current': 'MAXLC',
           'max_laser_power': 'MAXLP',
           'laser_output_power': 'P',
           'processor_temp': 'PST',
           'current_set_point': 'SC',
           'power_set_point': 'SP',
           'operating_status': 'STA',
           'software_version': 'SV',
           'alarm_reset': 'RST',
           'tec_enable': 'T',
           'digital_modulation_state': 'TTL'}

cmd_lcx = {'base_plate_temp': 'BT',
           'cdrh_state': 'CDRH',
           'fault_number': 'F',
           'cum_time_of_operation': 'HH',
           'serial_num_and_wavelength': 'HID',
           'interlock_state': 'INT',
           'input_voltage': 'IV',
           'key_state': 'KEY',
           'laser_emission_activation': 'L',
           'laser_output_power': 'P',
           'laser_output_power_percent': 'IP',
           'processor_temp': 'PST',
           'power_set_point': 'SP',
           'operating_status': 'STA',
           'software_version': 'SV',
           'contoller_reset': 'RST',
           'tec_enable': 'T',
           'port_configuration': 'CDC'}

laser_mode = {0: 'Constant power',
              1: 'Constant current',
              10:'Error in detection'}

fault = {0: 'No alarm',
         1: 'Diode current',
         2: 'Laser power',
         3: 'Power supply',
         4: 'Diode/Module temperature',
         5: 'Base temperature',
         6: 'Warning end of Life',
         7: 'Not interlocked',
         8: 'User-generated alarm',
         10:'Error in detection'}

operating_status = {1: 'Warm up',
                    2: 'Standby',
                    3: 'Laser ON',
                    4: 'Error',
                    5: 'Alarm',
                    6: 'Sleep',
                    7: 'Searching SLM point',
                    10:'Error in detection'}


class OxxiusController(object):

    def __init__(self):
        print("[UNILAB] OxxiusController.__init__() called", flush=True)
        self.comportName = ""
        self.baudrate = 192000
        self.ReceiveCallback = None
        self.isopen = False
        self.receivedMessage = None
        self.serialport = serial.Serial()
        self.command = dict()
        self.laser_mode_dict = laser_mode
        self.operating_status_dict = operating_status
        self.fault_dict = fault
        self.mode = 0  # initial mode constant power
        self.fault = 0
        self.operating_status = 0
        self.islaseron = False

    def isOpen(self):
        _unilab_logger.debug("[UNILAB] OxxiusController.isOpen() called")
        return self.isopen

    def Open(self, model_name):
        _unilab_logger.debug("[UNILAB] OxxiusController.Open() called")
        if model_name == '488':
            portname = 'COM5'
            self.command = cmd_lbx
        elif model_name == '561':
            portname = 'COM6'
            self.command = cmd_lcx
        if not self.isopen:
            try:
                self.serialport = serial.Serial(port=portname,
                                                baudrate=self.baudrate,
                                                parity=serial.PARITY_NONE,
                                                stopbits=serial.STOPBITS_ONE,
                                                bytesize=serial.EIGHTBITS,
                                                timeout=0.05,
                                                write_timeout=0.05,
                                                inter_byte_timeout=None,
                                                exclusive=True)
                self.isopen = self.serialport.isOpen()
                print("Laser is connected to:", portname)
            except:
                print("Error at opening COM port: ", sys.exc_info()[0])

    def Disconnect(self):
        _unilab_logger.debug("[UNILAB] OxxiusController.Disconnect() called")
        if self.isopen:
            try:
                self.serialport.close()
                self.isopen = self.serialport.isOpen()
                print("Laser is disconnected")
            except:
                print("Close error at closing COM port: ", sys.exc_info()[0])

    def Send(self, message):
        _unilab_logger.debug("[UNILAB] OxxiusController.Send() called")
        if self.isopen:
            try:
                newmessage = message.strip()
                newmessage += '\n'
                self.serialport.write(newmessage.encode('utf-8'))
                # sleep(0.005)
            except:
                print("Error sending message: ", sys.exc_info()[0], message)
            else:
                return True
        else:
            return False

    def Read(self, query_name):
        _unilab_logger.debug("[UNILAB] OxxiusController.Read() called")
        if self.isopen:
            message = '?' + self.command[query_name]
            self.Send(message)
            res = self.serialport.read_until('\r\n')
            # sleep(0.005)
            res = res.decode('utf-8')
            return res.replace('\r\n', '')
        else:
            return False

    def Set(self, cmd_name, value):
        _unilab_logger.debug("[UNILAB] OxxiusController.Set() called")
        if self.isopen:
            message = self.command[cmd_name] + '=' + str(value)
            self.Send(message)
            # return self.Read(cmd_name)
        else:
            return False

    def LaserOn(self):
        _unilab_logger.debug("[UNILAB] OxxiusController.LaserOn() called")
        if self.isopen:
            # if self.Read('laser_output_power')
            try:
                self.Set('laser_emission_activation', 1)
                return print("Laser ON")
            except:
                print("Error: ", sys.exc_info()[0])
        else:
            return print("The laser is not connected.")

    def LaserOFF(self):
        _unilab_logger.debug("[UNILAB] OxxiusController.LaserOFF() called")
        if self.isopen:
            try:
                self.Set('laser_emission_activation', 0)
                return print("Laser OFF")
            except:
                print("Error: ", sys.exc_info()[0])
        else:
            return print("The laser is not connected.")

    def activateLaser(self,value):
        _unilab_logger.debug("[UNILAB] OxxiusController.activateLaser() called")
        if self.isopen:
            try:
                self.Set('laser_emission_activation', int(value))
                if int(value)==1:
                    print("Laser ON.")
                    self.islaseron = True
                elif int(value)==2:
                    print("Laser on at low power")
                    self.islaseron = True
                elif int(value==0):
                    print("Laser OFF.")
                    self.islaseron = False
            except:
                print("Error: ", sys.exc_info()[0])
        else:
            return print("The laser is not connected.")
    # '''Set/Read laser control mode'''
    def setMode(self, value):
        _unilab_logger.debug("[UNILAB] OxxiusController.setMode() called")

        inv_dict = {v: k for k, v in self.laser_mode_dict.items()}
        self.Set('analog_control_mode', inv_dict[value])

    def getMode(self):
        _unilab_logger.debug("[UNILAB] OxxiusController.getMode() called")
        # get the mode: constant power/ constant current
        # print(self.Read('analog_control_mode'))
        ret = self.Read('analog_control_mode')
        try:
            self.mode = int(ret)
            return self.laser_mode_dict[self.mode]
        except:
            print('Error in reading')
            return self.laser_mode_dict[10]
    # '''Set/Read laser current'''
    def setCurrent(self, value):
        _unilab_logger.debug("[UNILAB] OxxiusController.setCurrent() called")
        if self.mode == 1 and self.fault == 0:
            self.Set('laser_diode_current', value)

    def getCurrent(self):
        _unilab_logger.debug("[UNILAB] OxxiusController.getCurrent() called")

        return self.Read('laser_diode_current')

    def getCurrentSetPoint(self):
        _unilab_logger.debug("[UNILAB] OxxiusController.getCurrentSetPoint() called")

        return self.Read('current_set_point')

    # '''Set/Read laser power'''
    def setPower(self, value):
        _unilab_logger.debug("[UNILAB] OxxiusController.setPower() called")
        if self.mode == 0 and self.fault == 0:
            self.Set('laser_output_power', value)
            # print('Laser switched')

    def getPower(self):
        _unilab_logger.debug("[UNILAB] OxxiusController.getPower() called")

        return self.Read('laser_output_power')

    def getPowerSetPoint(self):
        _unilab_logger.debug("[UNILAB] OxxiusController.getPowerSetPoint() called")

        return self.Read('power_set_point')

    # '''Set/Read analog modulation state'''
    def setAnalogModulation(self, value):
        _unilab_logger.debug("[UNILAB] OxxiusController.setAnalogModulation() called")
        self.Set('analog_modulation_state', int(value))

    def getAnalogModulation(self):
        _unilab_logger.debug("[UNILAB] OxxiusController.getAnalogModulation() called")

        return self.Read('analog_modulation_state')

    # '''Set/Read digital modulation state'''
    def setDigitalModulation(self, value):
        _unilab_logger.debug("[UNILAB] OxxiusController.setDigitalModulation() called")
        self.Set('digital_modulation_state', int(value))
        # print('SET DM')

    def getDigitalModulation(self):
        _unilab_logger.debug("[UNILAB] OxxiusController.getDigitalModulation() called")

        return self.Read('digital_modulation_state')

    # '''Read temperatures'''
    def getBaseTemperature(self):
        _unilab_logger.debug("[UNILAB] OxxiusController.getBaseTemperature() called")

        return self.Read('base_plate_temp')

    def getDiodeTemperature(self):
        _unilab_logger.debug("[UNILAB] OxxiusController.getDiodeTemperature() called")

        return self.Read('measured_diode_temp')

    # '''Read laser operating status'''
    def getOperatingStatus(self):
        _unilab_logger.debug("[UNILAB] OxxiusController.getOperatingStatus() called")
        ret = self.Read('operating_status')
        try:
            self.operating_status = int(ret)
            return self.operating_status_dict[self.operating_status]
        except:
            print('Error in reading:',ret)
            return self.operating_status_dict[10]

    # '''Read fault'''
    def getFault(self):
        _unilab_logger.debug("[UNILAB] OxxiusController.getFault() called")
        ret = self.Read('fault_number')
        try:
            self.fault = int(ret)
            return self.fault_dict[self.fault]
        except:
            print('Error in reading:',ret)
            return self.fault_dict[10]
    # '''Read interlock state'''
    def getInterlock(self):
        _unilab_logger.debug("[UNILAB] OxxiusController.getInterlock() called")
        return self.Read('interlock_state')

    # '''Read serial number and wavelength'''
    def getLaserInfo(self):
        _unilab_logger.debug("[UNILAB] OxxiusController.getLaserInfo() called")
        return self.Read('serial_num_and_wavelength')

__all__ = ["OxxiusController"]
