"""
Auto-generated by Build Agent
Original: EGGS_labrad/servers/spectrum_analyzers/RigolDSA800.py

[UNILAB DEBUG] 此驱动已注入调试日志
- 每个方法被调用时会打印日志
- 日志格式: [UNILAB] ClassName.method_name() called
- 查看日志: logging.getLogger("unilab.driver").setLevel(logging.DEBUG)
"""
from typing import Dict, Any, Optional
import logging
import warnings as _warnings
_warnings.filterwarnings("ignore", message=r"pkg_resources is deprecated as an API\..*")
import sys as _sys
import types as _types

# 兼容性：部分上游框架（例如 lantz）在 import-time 强制加载 Qt（PyQt4/PySide1），
# 在 headless/现代 Python 环境中通常不存在这些绑定，会导致导入直接失败。
# 这里预注入一个最小的 `lantz.utils.qt` shim，避免触发其 Qt loader。
if "lantz.utils.qt" not in _sys.modules:
    _qt_mod = _types.ModuleType("lantz.utils.qt")

    class _DummyQtCore:
        class QObject:
            def __init__(self, *_args, **_kwargs):
                # 兼容 QObject(parent=...) 等调用
                return None

        @staticmethod
        def Signal(*_args, **_kwargs):
            return object()

        @staticmethod
        def Slot(*_args, **_kwargs):
            def _decorator(fn):
                return fn

            return _decorator

    _qt_mod.QtCore = _DummyQtCore

    class _DummySuperQObject(_DummyQtCore.QObject):
        def __new__(cls, *_args, **_kwargs):
            return super().__new__(cls)

        def __init__(self, *_args, **_kwargs):
            # 允许 Driver(...) 传参，不触发 object.__init__ 的 TypeError
            return None

    _qt_mod.MetaQObject = type
    _qt_mod.SuperQObject = _DummySuperQObject
    _sys.modules["lantz.utils.qt"] = _qt_mod

# Unilab 驱动调试日志
_unilab_logger = logging.getLogger("unilab.driver.RigolDSA800Wrapper")
_unilab_logger.setLevel(logging.DEBUG)
if not _unilab_logger.handlers:
    _handler = logging.StreamHandler()
    _handler.setFormatter(logging.Formatter("[%(asctime)s] %(message)s"))
    _unilab_logger.addHandler(_handler)

# Vendored repo packages: EGGS_labrad
import sys as _sys
from pathlib import Path as _Path
_vendor_root = _Path(__file__).resolve().parent / "_vendor"
if _vendor_root.exists() and str(_vendor_root) not in _sys.path:
    _sys.path.insert(0, str(_vendor_root))

import numpy as np
from labrad.gpib import GPIBDeviceWrapper
from twisted.internet.defer import inlineCallbacks, returnValue


class RigolDSA800Wrapper(GPIBDeviceWrapper):

    # SYSTEM
    def __init__(self, *args, **kwargs):
        print("[UNILAB] RigolDSA800Wrapper.__init__() called", flush=True)
        super().__init__(*args, **kwargs)
    @inlineCallbacks
    def reset(self):
        _unilab_logger.debug("[UNILAB] RigolDSA800Wrapper.reset() called")
        yield self.write('*RST')

    @inlineCallbacks
    def clear_buffers(self):
        _unilab_logger.debug("[UNILAB] RigolDSA800Wrapper.clear_buffers() called")
        yield self.write('*CLS')

    @inlineCallbacks
    def autoset(self):
        _unilab_logger.debug("[UNILAB] RigolDSA800Wrapper.autoset() called")
        yield self.write(':SENS:POW:ATUN')

    @inlineCallbacks
    def operationComplete(self):
        _unilab_logger.debug("[UNILAB] RigolDSA800Wrapper.operationComplete() called")
        resp = self.query('*OPC?')
        returnValue(bool(int(resp)))


    # ATTENUATION
    @inlineCallbacks
    def preamplifier(self, status):
        _unilab_logger.debug("[UNILAB] RigolDSA800Wrapper.preamplifier() called")
        if status is not None:
            yield self.write(':SENS:POW:RF:GAIN:STAT {:d}'.format(status))
        resp = yield self.query(':SENS:POW:RF:GAIN:STAT?')
        returnValue(bool(int(resp)))

    @inlineCallbacks
    def attenuation(self, att):
        _unilab_logger.debug("[UNILAB] RigolDSA800Wrapper.attenuation() called")
        if att is not None:
            if (att >= 0) and (att <= 30):
                yield self.write(':SENS:POW:RF:ATT {:f}'.format(att))
            else:
                raise Exception('Error: RF attenuation must be in range: [0, 30].')
        resp = yield self.query(':SENS:POW:RF:ATT?')
        returnValue(float(resp))


    # FREQUENCY RANGE
    @inlineCallbacks
    def frequencyStart(self, freq):
        _unilab_logger.debug("[UNILAB] RigolDSA800Wrapper.frequencyStart() called")
        if freq is not None:
            if (freq >= 0) and (freq <= 7.5e9):
                yield self.write(':SENS:FREQ:STAR {:f}'.format(freq))
            else:
                raise Exception('Error: start frequency must be in range: [0, 7.5e9].')
        resp = yield self.query(':SENS:FREQ:STAR?')
        returnValue(float(resp))

    @inlineCallbacks
    def frequencyStop(self, freq):
        _unilab_logger.debug("[UNILAB] RigolDSA800Wrapper.frequencyStop() called")
        if freq is not None:
            if (freq > 0) and (freq < 7.5e9):
                yield self.write(':SENS:FREQ:STOP {:f}'.format(freq))
            else:
                raise Exception('Error: stop frequency must be in range: [0, 7.5e9].')
        resp = yield self.query(':SENS:FREQ:STOP?')
        returnValue(float(resp))

    @inlineCallbacks
    def frequencyCenter(self, freq):
        _unilab_logger.debug("[UNILAB] RigolDSA800Wrapper.frequencyCenter() called")
        if freq is not None:
            if (freq > 0) and (freq < 7.5e9):
                yield self.write(':SENS:FREQ:CENT {:f}'.format(freq))
            else:
                raise Exception('Error: center frequency must be in range: [0, 7.5e9].')
        resp = yield self.query(':SENS:FREQ:CENT?')
        returnValue(float(resp))

    @inlineCallbacks
    def frequencySpan(self, span):
        _unilab_logger.debug("[UNILAB] RigolDSA800Wrapper.frequencySpan() called")
        if span is not None:
            if (span > 0) and (span < 7.5e9):
                yield self.write(':SENS:FREQ:SPAN {:f}'.format(span))
            else:
                raise Exception('Error: frequency span must be in range: [0, 7.5e9].')
        resp = yield self.query(':SENS:FREQ:SPAN?')
        returnValue(float(resp))


    # AMPLITUDE
    @inlineCallbacks
    def amplitudeReference(self, ampl):
        _unilab_logger.debug("[UNILAB] RigolDSA800Wrapper.amplitudeReference() called")
        if ampl is not None:
            if (ampl >= -100) and (ampl <= 20):
                yield self.write(':DISP:WIN:TRAC:Y:SCAL:RLEV {:f}'.format(ampl))
            else:
                raise Exception('Error: display reference value must be in range: [-100, 20].')
        resp = yield self.query(':DISP:WIN:TRAC:Y:SCAL:RLEV?')
        returnValue(float(resp))

    @inlineCallbacks
    def amplitudeOffset(self, ampl):
        _unilab_logger.debug("[UNILAB] RigolDSA800Wrapper.amplitudeOffset() called")
        if ampl is not None:
            if (ampl > -300) and (ampl < 300):
                yield self.write(':DISP:WIN:TRAC:Y:SCAL:RLEV:OFFS {:f}'.format(ampl))
            else:
                raise Exception('Error: display offset must be in range: [-300, 300].')
        resp = yield self.query(':DISP:WIN:TRAC:Y:SCAL:RLEV:OFFS?')
        returnValue(float(resp))

    @inlineCallbacks
    def amplitudeScale(self, factor):
        _unilab_logger.debug("[UNILAB] RigolDSA800Wrapper.amplitudeScale() called")
        if factor is not None:
            if (factor > 0.1) and (factor < 20):
                yield self.write(':DISP:WIN:TRAC:Y:SCAL:PDIV {:f}'.format(factor))
            else:
                raise Exception('Error: display scale must be in range: [0.1, 20].')
        resp = yield self.query(':DISP:WIN:TRAC:Y:SCAL:PDIV?')
        returnValue(float(resp))


    # MARKER SETUP
    @inlineCallbacks
    def markerToggle(self, channel, status):
        _unilab_logger.debug("[UNILAB] RigolDSA800Wrapper.markerToggle() called")
        if status is not None:
            yield self.write(':CALC:MARK{:d}:STAT {:d}'.format(channel, status))
        resp = yield self.query(':CALC:MARK{:d}:STAT?'.format(channel))
        returnValue(bool(int(resp)))

    @inlineCallbacks
    def markerTrace(self, channel, trace):
        _unilab_logger.debug("[UNILAB] RigolDSA800Wrapper.markerTrace() called")
        if trace is not None:
            yield self.write(':CALC:MARK{:d}:TRAC {:d}'.format(channel, trace))
        resp = yield self.query(':CALC:MARK{:d}:TRAC?'.format(channel))
        returnValue(int(resp))

    @inlineCallbacks
    def markerMode(self, channel, mode):
        _unilab_logger.debug("[UNILAB] RigolDSA800Wrapper.markerMode() called")
        modeConvert = {0: 'POS', 1: 'DELT', 2: 'BAND', 3: 'SPAN'}
        modeInvert = {val: key for key, val in modeConvert.items()}
        if mode is not None:
            mode = modeConvert[mode]
            yield self.write(':CALC:MARK{:d}:MODE {:s}'.format(channel, mode))
        resp = yield self.query(':CALC:MARK{:d}:MODE?'.format(channel))
        returnValue(modeInvert[resp])

    @inlineCallbacks
    def markerReadoutMode(self, channel, mode):
        _unilab_logger.debug("[UNILAB] RigolDSA800Wrapper.markerReadoutMode() called")
        modeConvert = {0: 'FREQ', 1: 'TIME', 2: 'ITIM', 3: 'PER'}
        modeInvert = {val: key for key, val in modeConvert.items()}
        if mode is not None:
            mode = modeConvert[mode]
            yield self.write(':CALC:MARK{:d}:X:READ {:s}'.format(channel, mode))
        resp = yield self.query(':CALC:MARK{:d}:X:READ?'.format(channel))
        returnValue(modeInvert[resp])

    @inlineCallbacks
    def markerTrack(self, channel, status):
        _unilab_logger.debug("[UNILAB] RigolDSA800Wrapper.markerTrack() called")
        if status is not None:
            yield self.write(':CALC:MARK{:d}:TRAC:STAT {:d}'.format(channel, status))
        resp = yield self.query(':CALC:MARK{:d}:TRAC:STAT?'.format(channel))
        returnValue(bool(int(resp)))


    # MARKER READOUT
    @inlineCallbacks
    def markerAmplitude(self, channel):
        _unilab_logger.debug("[UNILAB] RigolDSA800Wrapper.markerAmplitude() called")
        resp = yield self.query(':CALC:MARK{:d}:Y?'.format(channel))
        returnValue(float(resp))

    @inlineCallbacks
    def markerFrequency(self, channel, freq):
        _unilab_logger.debug("[UNILAB] RigolDSA800Wrapper.markerFrequency() called")
        if freq is not None:
            if (freq > 0) and (freq < 1.5e9):
                yield self.write(':CALC:MARK{:d}:X {:f}'.format(channel, freq))
            else:
                raise Exception('Error: marker frequency must be in range: [0, 1.5e9].')
        resp = yield self.query(':CALC:MARK{:d}:X?'.format(channel))
        returnValue(float(resp))


    # MARKER-RELATED PEAK FUNCTIONS
    @inlineCallbacks
    def peakSearch(self, status):
        _unilab_logger.debug("[UNILAB] RigolDSA800Wrapper.peakSearch() called")
        if status is not None:
            yield self.write(':CALC:MARK:CPE:STAT {:d}'.format(status))
        resp = yield self.query(':CALC:MARK:CPE:STAT?')
        returnValue(bool(int(resp)))

    @inlineCallbacks
    def peakSet(self, channel):
        _unilab_logger.debug("[UNILAB] RigolDSA800Wrapper.peakSet() called")
        yield self.write('CALC:MARK{:d}:MAX:MAX'.format(channel))

    @inlineCallbacks
    def peakNext(self, channel):
        _unilab_logger.debug("[UNILAB] RigolDSA800Wrapper.peakNext() called")
        yield self.write('CALC:MARK{:d}:MAX:NEXT'.format(channel))


    # PEAKS ONLY
    def peakThreshold(self, c, threshold):
        _unilab_logger.debug("[UNILAB] RigolDSA800Wrapper.peakThreshold() called")
        raise NotImplementedError

    def peakExcursion(self, c, excursion):
        _unilab_logger.debug("[UNILAB] RigolDSA800Wrapper.peakExcursion() called")
        raise NotImplementedError

    def peakTable(self, c):
        _unilab_logger.debug("[UNILAB] RigolDSA800Wrapper.peakTable() called")
        # parse response
        resp = yield self.query(':TRAC:MATH:PEAK?')
        resp = [float(val) for val in resp.split(',')]

        # separate amplitude and frequency
        amp_list = resp[1::2]
        freq_list = resp[0::2]

        return list(zip(freq_list, amp_list))


    # BANDWIDTH
    @inlineCallbacks
    def bandwidthSweepTime(self, time):
        _unilab_logger.debug("[UNILAB] RigolDSA800Wrapper.bandwidthSweepTime() called")
        if time is not None:
            if (time > 2e-6) and (time < 7500):
                yield self.write(':SENS:SWE:TIME {:f}'.format(time))
            else:
                raise Exception('Error: sweep time must be in range: [2e-6, 7500].')
        resp = yield self.query(':SENS:SWE:TIME?')
        returnValue(float(resp))

    @inlineCallbacks
    def bandwidthResolution(self, bw):
        _unilab_logger.debug("[UNILAB] RigolDSA800Wrapper.bandwidthResolution() called")
        if bw is not None:
            if (bw > 10) and (bw < 1e7):
                yield self.write(':SENS:BAND:RES {:f}'.format(bw))
            else:
                raise Exception('Error: resolution bandwidth must be in range: [10, 1e7].')
        resp = yield self.query(':SENS:BAND:RES?')
        returnValue(int(resp))

    @inlineCallbacks
    def bandwidthVideo(self, bw):
        _unilab_logger.debug("[UNILAB] RigolDSA800Wrapper.bandwidthVideo() called")
        if bw is not None:
            if (bw > 10) and (bw < 1e7):
                yield self.write(':SENS:BAND:VID {:f}'.format(bw))
            else:
                raise Exception('Error: video bandwidth must be in range: [10, 1e7].')
        resp = yield self.query(':SENS:BAND:VID?')
        returnValue(int(resp))


    # TRACE
    @inlineCallbacks
    def getTrace(self, channel):
        _unilab_logger.debug("[UNILAB] RigolDSA800Wrapper.getTrace() called")
        # set data format
        yield self.write(':FORM:TRAC:DATA ASC')

        # get data
        data = yield self.query(':TRAC:DATA? TRACE{:d}'.format(channel))
        data = self._processData(data)

        # create x-axis
        freq_start = yield self.query(':SENS:FREQ:START?')
        freq_stop = yield self.query(':SENS:FREQ:STOP?')
        xAxis = np.linspace(int(freq_start), int(freq_stop), len(data))

        returnValue((xAxis, data))


    # HELPER
    def _processData(self, data):
        _unilab_logger.debug("[UNILAB] RigolDSA800Wrapper._processData() called")
        """
        Process data for header and separate data values.
        """
        # header is in #NXXXXXXXXX format
        tmc_N = int(data[1])

        # remove header and split data
        processed_data = np.array(data[2 + tmc_N:].split(', '), dtype=float)
        return processed_data

__all__ = ["RigolDSA800Wrapper"]
