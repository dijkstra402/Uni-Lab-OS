message_server_interface:
  class:
    module: unilabos.devices.community.message_server_interface.driver:MessageServerInterface
    type: python
    status_types: {}
    action_value_mappings: {}
  description: MessageServerInterface
  category:
  - custom
  init_param_schema:
    config:
      type: object
      properties:
        ipaddr:
          type: string
          default: localhost
        targetSocket:
          type: string
          default: 1412
